/**
 * Site-wide constants in one place. Renaming the project means editing here
 * (and `vite.config.ts`'s `REPO`, which drives the deploy base path).
 */

/** GitHub repository, used for the source links in the masthead and footer. */
export const REPO_URL = 'https://github.com/CostardRouge/atelier';
