# Project memory — decisions, reasons, traps

Long-term memory of this repo, read at the start of **every** agent session (imported by `CLAUDE.md`). It holds what the code and `git log` cannot tell you: the choices made and their reasons, what was tried and rejected, the traps that cost time, how the maintainer likes to work.

This file is the **always-loaded index**. The detail lives in `docs/memory/<topic>.md`, one file per area, loaded **on demand**: read the topic file(s) matching the area you are about to touch **before** acting (table at the bottom). Do not `@import` them into `CLAUDE.md` — the split exists to keep the per-session prompt small.

## How to maintain (mandatory — CLAUDE.md rule 2)

- **When**: at the end of every task, before its commit, in the same commit. Writing is the **default**; only skip if there is truly nothing a future agent could use, and say so explicitly in the final message.
- **What**: a design/product decision, a non-obvious technical choice, an explicit rejection ("the maintainer did not want X because Y"), a trap (browser, tooling, framework, hosting) and its remedy, a working preference. Not implementation detail readable in the diff, not what `git log` already says, not history ("this was fixed on…") — once a fix is committed, keep only the rule it taught.
- **Where**: the matching `docs/memory/<topic>.md`; a new file only when no topic fits (kebab-case name, add it to the table below with a "read when"). Cross-cutting rules, working style, decisions-at-a-glance and open items stay in this index.
- **How**: search first and **update** the existing entry rather than adding a near-duplicate; delete what became false. One entry = one short paragraph: *decision → why → how to apply*, dated `YYYY-MM-DD` on first write and on each revision. Say the same thing **once** — cross-reference other files by name instead of repeating.
- **Language**: **English**, dense, factual. No session narration.
- Budget: keep this index under ~200 lines and each topic file under ~150; if one outgrows that, split it.

## Working with Steeve Pommier

<!-- Add what you learn about how he validates work, how he phrases requests,
     what he wants when an audit finds problems, what annoys him. -->

- 2026-08-20 — Observed from history, not yet confirmed in conversation: work lands as focused, self-describing commits (one tool or one fix per commit, prose imperative titles, a body explaining the *why*), and most agent work reaches `main` through a pull request (`(#NN)` merge titles) rather than a direct push. Assume a PR is expected for anything non-trivial in a cloud session.
- 2026-09-09 — **Never size work in human developer days.** The agent writes the code, in this session; the maintainer's words: *"you are going to do as you did all of my development across multiple of my repos during the last four months — this is not going to take days, it could be done tonight if I instruct you right"*. A "one week" estimate on a feature reads as a refusal to start. **How to apply**: size a plan in COMMITS and passes (one commit = one task, CLAUDE.md rule 1), name what each one delivers and what verifies it, and let him stop the run wherever he wants. The scarce resource is his instruction and his review, never engineering time.
- 2026-08-20 — Documentation is kept current with the code: the README describes every tool, its trade-offs and its browser caveats, and a commit that changed the tool line-up also refreshed the README (`aade8e2`). Treat a user-visible feature as unfinished until the README says the same thing as the code.

## Direction in five lines

- **Local-first, no server, no account**: files are read in the browser and never uploaded. Every feature must hold this line; the documented exception is the Flight Map's opt-in OpenStreetMap base layer, off by default and surfaced explicitly (README, "The one network exception"). The full list of requests the app actually makes is in `local-first.md` — it is longer than that callout.
- **A suite converging into one Studio**: a thin shell (`src/app/`) plus self-contained tools (`src/tools/*`) over a generic core (`src/shared/*`), all listed in one registry (`src/app/tools.tsx`) — and, since 2026-08-20, an agreed plan to merge the tools into a single `/studio` editor (phases and decisions in `studio.md`).
- **Capture-oriented**: photo and video across devices (DJI, Apple, Sony), with DJI flight telemetry as the founding case.
- **The browser is the runtime today, not forever**: pure logic is kept DOM-free so a native shell (Tauri, bundled ffmpeg) can reuse it — `shared/sources/file-sources.ts` is meant to be the only brick that changes.
- **Visual identity is deliberate**: "Studio Papier" — ink on warm cream, one vermilion accent, monospace numerals (`src/index.css` tokens).

## Decisions at a glance (details in the topic files)

- One tool registry drives nav, routes and the asset sidebar; adding a tool is one entry plus a component — `architecture.md`.
- `shared/` never imports `tools/`; pure logic lives in DOM-free modules with unit tests beside them — `architecture.md`, `testing.md`.
- One global asset library keyed by base name feeds every tool through capability matching — `architecture.md`.
- Video export is one shared WebCodecs pipeline (`exportProcessedVideo`) parameterised by a per-frame processor; audio is copied, never re-encoded — `media-pipeline.md`.
- Audio the suite MAKES (a hook's tick bed) IS encoded, to AAC, before the muxer is built — and rendered ahead of the encoder's measured 2112-sample priming, because mp4-muxer writes no edit list and refuses negative timestamps; sync is claimed only after decoding the MP4 back. A clip's own sound is still COPIED by default: the bed becomes the track only of a silent or re-timed clip, and is mixed into a clip's sound only on request (`audio-plan.ts`) — `media-pipeline.md`.
- HEVC that the browser cannot decode is handled by an opt-in, in-browser ffmpeg.wasm transcode to H.264, not by uploading or by dropping the clip — `media-pipeline.md`.
- Export frame rate is a per-variant resample onto a `1/fps` grid — duration kept, frames dropped or duplicated, never interpolated; asking for the source rate stays an exact pass-through — `media-pipeline.md`, `studio.md`.
- The GitHub Pages base path is derived from `GITHUB_REPOSITORY`, never hardcoded — `deployment.md`.
- Every tool is its OWN CHUNK, cut at the registry with `React.lazy` (2026-09-22): the entry script went from 1.9 MB to 511 kB, a stale chunk after a deploy reloads once through `vite:preloadError`, and a heavy dependency the shell can see statically (mp4box was) goes behind a dynamic import at its point of use — `deployment.md`.
- Built-in LUTs are discovered by a Vite virtual module scanning `public/luts/` — no manual list — `deployment.md`. Their PICKER TILES, by contrast, are baked by a script run by hand and committed (`scripts/gen-lut-thumbs.mjs` → `public/lut-thumbs/`), like the icons and the gazetteer, so the gallery opens without fetching a single `.cube` — `deployment.md`, `media-pipeline.md`.
- A `.cube` the user owns — uploaded or bought — lives in a VAULT (its own IndexedDB database, optionally kept on a Winnow), and a document stores a REFERENCE to it, never a lattice; an upload has not inlined one since 2026-09-20. ★ favourites are a `localStorage` shortlist across every family — `media-pipeline.md`, `local-first.md`, `docs/lut-packs.md`.
- A look carries TWO hashes: the source `.cube`'s (the vault's key, a reference's identity) and its ENCODED lattice's (`PackLook.blob`, the file store's key, measured from the bytes as they are sent). A content-addressed bucket verifies the body against the path, so an id is never carried over from something else — uploading under the text's hash refused every look with a 400 — `media-pipeline.md`, `docs/lut-packs.md` §4.3.
- MapLibre is dynamically imported (JS + CSS) so it stays out of the main bundle — `frontend.md`.
- The overlay engine (element model, stage, burn-in export, editing panels) is shared (`shared/overlay/`), consumed by both the Studio and the legacy overlay page — `architecture.md`, `studio.md`.
- Overlay elements are added from a foldable preview palette, never a dropdown; a preview shows the real value or nothing, never a fabricated one — `studio.md`.
- Motion travels on ONE curve registry (`shared/motion/easing.ts`), read by the overlay engine and the openers alike: additive string ids, overshooting curves clamp opacity and never reach an opener (no inverse), `steps` is the one curve with a number; entrance DELAYS are derived from boxes by `overlay/stagger.ts` (dense ranks, ties, seeded random), never stored on an element — `roadtrip.md`.
- A slide may hold SEVERAL pictures (`collage.ts`, v24): cell 1 IS the slide, so every one-picture reader keeps working; an empty cell draws nothing in the export; a smaller layout keeps the extra pictures; the inspector, the Library and the develop sheet follow the SELECTED cell — `roadtrip.md`.
- A picture is DRAGGED onto a cell from any Library row or instance tile: the drag item lives in memory (`asset-drag.ts`, a `resolve()` that fetches an instance tile on drop), the stage says where it will land and that it landed (`DropZones`), a drop WRITES before it selects, the drop is accepted on `dragenter` too, and the page ends a drag whose source vanished — `roadtrip.md`; how to drive it in the pane — `testing.md`.
- Canvas trap: a shadow is dropped when drawing under a `destination-*` composite mode — build masks in their own buffer first — `studio.md`.
- The heading is course over ground (no compass in the log) and vanishes while hovering or yawing; smoothing is a pure `(cues, time)` function, never a per-frame accumulator — `studio.md`.
- The clip's opening window carries the same motion measured **forward** (`Cue.lead`, read through `motionAt`), so the instruments are alive on frame one; it never covers a real reading, never reaches past that window, and invents nothing — `studio.md`.
- Tool layouts split on **container** queries, not viewport ones: the Library sidebar eats 288px the viewport query cannot see — `studio.md`.
- The SHELL, by contrast, wears three named layouts decided in one DOM-free module and published like a scope — `compact` / `medium` / `expanded`, split at 820 (unchanged, so adoption is additive) and 1180; a tool reads the name to know whether it draws its own tabs or hands its sections to the shell's bar, never to lay out its own columns — `frontend.md`.
- Light ink ON a picture (a tile's name, a chip over the stage, a label on `frame`) is the FIXED `on-media` token, never `paper` (near-black at night); a chip over a picture is `bg-surface/NN` so its ground flips with its ink; a fixed paper rgba in a className is a lint error (2026-09-23) — `frontend.md`.
- A gutter between a FIXED edge and a scroll container is paper the content gets clipped against, never spacing: breathing room goes inside the scroller, where it scrolls away — `frontend.md`.
- The shell keeps a **definite screen height at every width**, phones included: giving it up under 820px is what made the suite a vertical stack AND what made every height above a stage indefinite, so one fixed height retires that bug class instead of patching it per tool — `frontend.md`.
- On a phone the library is a **sheet at its tallest rest, one cell of the bottom bar among the others** — the dock that gave it half the height was tried and reversed, because neither half was enough and a menu whose cells behave differently is the worse fault. What survives: a stage on a compact shell FLEXES (and only if its whole chain does), and a picker's row is a TILE whose big target SETS the picture while a corner verb looks at it — `frontend.md`.
- The library stays **docked at the left everywhere but a phone**, collapsed to its 48px rail by default below 1180px — the maintainer's call, and the rail is what reconciles it with a tool's own 800px container query, which a 288px column starves on a tablet. A phone gets a `BottomSheet` instead. A tool's SECTIONS are published to the shell and drawn in the thumb zone on a phone, from the same array its docked tab strip uses, with the shell's own **Library cell first on every screen** — a phone has ONE bottom menu, so the bar is drawn even for a tool that publishes no sections and there is no app-bar burger anywhere — `frontend.md`.
- Time elements: presentation per badge, capture-time **shift** per project; no timezone picker — the log has no zone to convert from — `studio.md`.
- Conversion LUTs target a Rec.709 reference display (~gamma 2.4) while the canvas is sRGB (~2.2); an optional output transform closes that gap, baked into the composed LUT, off by default — `media-pipeline.md`.
- LUT lattice lookup is tetrahedral by default (neutrals stay neutral), trilinear on request; the mode is a localStorage render pref and must be used by the bake AND the shader — `media-pipeline.md`.
- `DevelopSettings.base` is a LADDER of four rungs (2026-09-20) — `proxy` · `gain` · `gainMap` · `gainMapWarp` — each a real and nameable amount of the camera's own calibration, read from the DNG's `OpcodeList3` and never invented: a rung is offered only where the file carries the opcode, `proxy` is the absence of a base (so `render` → null and `raw` → `gain` migrated nothing), and the PREVIEW carries the calibration from `gain map` up so preview = export holds by construction. The ladder hangs off the fidelity chip, whose menu since 2026-09-21 is the LIST OF THE CAPTURE'S FILES with the rungs nested under the sensor (`renditions.md`) — `raw.md`, `render-gain-map.md`, `render-geometry.md`.
- A RAW is developed from the SENSOR's data on request, per picture (`DevelopSettings.base: 'raw'`, since 2026-09-20): `libraw-wasm` in its own worker, dynamically imported, working without cross-origin isolation, decoded to linear light and handed to the graph as a half-float source; the ONE cube survives as the develop, its `[0,1]` being the sensor's range and a MEASURED gain (`rawGain`, stored so preview = export) bringing the picture to its own exposure before the sliders — which is what makes highlight recovery real. The base travels nowhere (no preset, paste or batch carries it); the 8-bit embedded render stays the default — `raw.md`.
- A RAW's two halves are turned by TWO different hands and must agree (2026-09-22): the SENSOR rung is upright because `userFlip` is left alone and LibRaw's `-1` default applies the camera's flip, while the camera RENDER is a byte-exact slice taken out of the container, leaving the Orientation tag behind — so it is GIVEN one, a 26-byte Exif block spliced into the JPEG that the browser then honours at decode, never a second 131 MB bitmap. Three guards refuse a double turn; a landscape capture pays nothing; `buildExifBlock` still writes `Orientation = 1` on every delivery. And `rawSizes()` now reports the pixels as SHOWN, while the IFD readers stay the file's own unrotated statement — `media-pipeline.md`, `raw.md`, `develop.md`.
- DETAIL — denoise, defringe, sharpen — is four classic fragment passes with pure twins the gate holds them to (2026-09-20): chroma blurred alone, a luma bilateral, purple pulled to neutral only at a steep edge, an unsharp mask on luma applied as one ratio; kernels in SOURCE pixels (the stage scales them, the loupe judges them); noise runs BEFORE the cube on the source, sharpen LAST after every warp — `render-detail.md`.
- REPAIR (2026-09-20) is ONE patch list (`RollPicture.repair`, destination disc + source offset + heal/clone) drawn as ONE pass FIRST on the source, so a copied pixel takes every later stage with its neighbours; a heal matches the destination's SURROUNDINGS (a ring, never the spot's own mean — that mean IS the dust); dust is detected on the stage's decode and never stored; a tap heals from beside, a drag says where from; `patch` is a reserved GLSL word that refuses a pass silently, so the render gate now relays the page's console errors — `render-repair.md`.
- HDR on a STILL is an **Ultra HDR JPEG** (2026-09-20): the SDR file with a gain map inside it, MEASURED from a second render of the RAW developed the asked stops darker (never invented: where the SDR had room the map is flat, the midtones never move, and a render — nothing above white — is refused the map and told so), wrapped in a hand-written XMP + MPF container, and called Ultra HDR only after the file is read back and its map, numbers and peak match what was written. The stage stays the SDR base: no browser grants an HDR canvas without a flag, and the panel SAYS what the display and the canvas can do rather than drawing a preview that pretends — `hdr.md`.
- The cube TEXTURE is float, half-float where the GPU cannot filter float, never 8-bit (an 8-bit cube clamps a conversion LUT's rolloff above white), uploaded by ONE function for both shaders; the render gate forces the half-float path by hand — `render-core.md`.
- A picture's CORRECTION (a *develop*: exposure, tone bands, white balance, saturation, vibrance — `shared/develop/develop.ts`) is the FIRST stage baked into the one cube, before every look and the output transform; it is never inherited by the next picture, an untouched pixel comes back bit-identical, and a source developed at decode (a RAW) grades through `composedForDeveloped` so the numbers never apply twice — `media-pipeline.md`, `docs/photo-develop.md`. Since 2026-09-17 it also carries the five CURVES and the per-channel LEVELS of `shared/develop/curves.ts` (`luma` as a ratio so a grey stays grey, `rgb` and the three channels directly because tinting is what they are for; monotone cubic, display-referred, the 33 floor kept on measured error) — which is what made the record stop being flat numbers, so a keeper clones through `cloneDevelop` and equality is `sameDevelop`, never a spread and never `===` — `media-pipeline.md`. In Trips it is per SLIDE (v17), every renderer takes its slide's own cube from `LutStack.composeWith`, and ONE `DevelopSheet` (whose draft rides `stack.develop`) opens from the Picture tab's settled row — `roadtrip.md`. In the Studio it is per MEDIA in the bound half (`ProjectMedia.develops`, v15), keyed like a trim, guarded by the file's hash, re-keyed by `adoptRenames`, never in `.atelier.json`, opened from the Grade tab's settled row over a photo or a clip — `studio.md`.
- Which pixels a STILL is delivered from is one question with one answer everywhere (2026-09-20, O2) — **and since 2026-09-21 that answer lives on the PICTURE, not at the export door**, because a rendition names the bits and the frame at once (`renditions.md`, `docs/capture-renditions.md` §13.2, his pick: *"garde la case à cocher"*). **In Develop (R4, built)**: a roll's picture leaves from its own rendition — its RAW on the sensor, the file set above the photograph, else where it opens with `Auto`'s arithmetic unnamed —, `RollExport.originals` is off the document (roll v4), the panel says the whole run before a byte moves (`run-plan.ts`), and the door keeps ONE switch, *Proxies only, for this run*, which is the editor's and never the roll's. **In Trips and the Studio (R5, built the same day)**: NO control at all — `Auto`'s arithmetic unnamed over ONE seam (`fixedFrameDelivery` for a host whose frame is a frame, `deliveryFor` to fetch and hold, `useDeliveryRow` to say it) and a *Delivers* row that names the original by its FILE, the pill's own word; the three words are gone from every host, `'proxies'` survives only for Develop's switch, and forcing the original from a host without a pill is the one capability given up. Trips wraps `renderDeck`'s injected resolver rather than teaching it about sources, and the Studio's stills take the path its clips always did — `roadtrip.md`, `studio.md`, `develop-roll.md`, `renditions-build.md`.
- A RAW original is delivered from the render INSIDE it, and only when that render beats the proxy — measured, never assumed (2026-09-20, correcting `develop-originals.md` §7.4, whose "full-size embedded preview, else the proxy" is false for the camera the maintainer uses): the render's size is read from a megabyte of the original's head and cached for the session, the *Delivers* row and the run read the same cache so they cannot disagree, and `Auto` never downloads 74 MB to deliver 0.52 megapixels. The SENSOR is reached by developing on `base: 'raw'`, never by the export — `develop-roll.md`, `raw.md`.
- A picture says its PIXELS and not only its bits (2026-09-20): `pictureFidelity` carries the frame and the megapixels, and the shortfall against the file's own pixels where they are known — a DJI DNG reads `RAW · camera render · 960 × 540`, *8.4× short on the long edge of its 8064 × 4536*, because the render inside that file IS 0.5 of 36.6 megapixels. Nothing is claimed that was not measured, an ordinary original is never decoded a second time to caption it, and the *Delivers* row calls those pixels `Camera render` — `develop.md`, `develop-roll.md`.
- The editor stage works to a PIXEL BUDGET (one 4K frame), never the media's own density: a 48-megapixel still put three 194 MB buffers up the moment a LUT was picked, and killed the tab on an iPhone. A preview budget only — every deliverable still composes from the source bitmap — `media-pipeline.md`.
- A RAW is the ONE source decoded in the tab's own memory, so it is where a phone's tab dies — and it did, on every DNG (2026-09-23). The decode's plane is now converted in BANDS with nothing full-size in between (bit-identical to the old two-step path, pinned by spec; the stage's post-processing fell from +170 MB to +22 MB), the main thread is given back between bands, a CONSTRAINED device (`lib/device-class.ts`: iOS, Android, ≤ 4 GiB — coarse on purpose, overridable in `localStorage['atelier.device']`) decodes to a long edge per purpose (stage 2560, export 4096, the loupe never whole and SAYING so), lets the decoder's 300 MB heap go after 8 s and on a hidden tab, and holds decodes and fetched originals under small ceilings. Measured in node and driven headless with the class forced; NOT yet on his phone — `device-memory.md`.
- A preview grades a picture ONCE (`holdGrades`), not per repaint, and draws a raster copy of it: a WebGL canvas drawn into a 2D canvas costs a copy per draw that grows with its size, which is what made a graded Trips stage crawl on every drag; a clip's owner invalidates per frame — `media-pipeline.md`.
- Exports **already** carry a correct `colr` bt709 tag, via encoder metadata mp4-muxer turns into the box — measured; do not "add" tagging, and a guard drops any colour space the muxer would mis-encode — `media-pipeline.md`.
- **No CI gate can see a broken shader** — GLSL is a template literal nothing compiles, and failure degrades silently to un-graded exports. Run `node scripts/check-shader.mjs` after touching `lut-gl.ts` — `media-pipeline.md`.
- HDR (HLG/PQ) is not handled in VIDEO and deliberately will not be: the pipeline is 8-bit SDR by construction and a browser cannot reasonably do better — `media-pipeline.md`. **For STILLS that ruling is reversed (2026-09-17)**: a photograph needs neither WebCodecs nor a High 10 encode, only float textures and a gain-map container, so the photo editor carries headroom end to end and delivers Ultra HDR JPEG — `docs/photo-editor.md` §4.4.
- A FILM STOCK (a Fujifilm-style simulation) is a LOOK layer generated from numbers (`shared/film/emulsion.ts`, `source: 'film'`, the settings in the layer's `customText`), never a stage beside the develop — because a conversion LUT must run before it and only the layer list is ordered by the author; its colour lives in the cube with no migration, it tints greys on purpose, mid grey is exact on every stock, 33³ is measured — `media-pipeline.md`, `docs/film-simulation.md`.
- A stock's GRAIN and HALATION are ONE node of the render graph (`FilmNode`, 2026-09-20), never a second engine in `lut-gl.ts`: it sits LAST, in a position no caller chooses, so a clip takes `SOURCE → CUBE → [FILM] → OUTPUT`; the texture lives on the GRADE beside the layers and cascades with the rung; a grain cell is a fraction of the frame's HEIGHT read bilinearly from a tile, so the export's grain IS the preview's resampled (measured, 0 codes at two sizes); the field re-rolls per SOURCE frame quantised to `grainFps`, never per rAF; `gradeKey` and `holdGrades` fold it in or the slider is dead on a still; and a grained clip is encoded at ~0.18 bpp, since a new noise field every frame is nothing a P-frame can predict — `render-film.md`.
- The suite's render engine is ONE baked cube, global and 8-bit, and that is exactly what ten of the fourteen asks of a real photo editor cannot ride: a cube is handed no coordinate, no neighbour and no headroom. The answer agreed 2026-09-17 is a float16 multi-pass GPU graph in which the cube SURVIVES as two nodes (the global develop, the look), so the maths stays node-tested, an unlayered picture keeps today's one-cube path byte for byte, and the seven consumers change at ONE seam — `FrameGrader.render` — `docs/photo-editor.md`.
- In/out trimming is **per clip** (bound half of the project, keyed by media name and guarded by duration) and the export cuts inside the one WebCodecs pipeline — `studio.md`, `media-pipeline.md`.
- A project's settings export to / import from `.atelier.json` — the portable half exactly, media and trims never; export + overwrite-this-project live in the settings modal, import-as-new-project in the gallery — `studio.md`.
- A trip exports to / imports from `.roadtrip.json`, and it is a **backup, not a template**: the whole document minus the trip id, the timestamps, its `sourceId` and each post's `projectId`; media refs travel because their hash is what finds the pictures elsewhere; import always makes a new trip, on the source that imports it — `roadtrip.md`.
- `TripDoc.sourceId` (v11) names the ONE source a trip is kept in, like `ProjectDoc`'s; the trip gallery groups by source even with one group, and the New-trip "Keep on" picker only appears with a second document-capable source — `roadtrip.md`, `docs/roadtrip-persistence.md`.
- A trip kept on a Winnow saves **local now, remote on idle**: the remote copy is the authority, the IndexedDB copy its mirror, the bookkeeping a reducer beside the document (`shared/sources/doc-sync.ts`, store `sync`), an etag that REFUSES a stale write and a pill that says every state — never a sync engine. The Winnow side (migration 0041 + `/api/apps/:app/docs`) shipped there on 2026-09-07 and the whole flow is confirmed working; a browser that connected BEFORE it shipped hides the feature until `reconnect`, because the capabilities sheet is a snapshot — `roadtrip.md`, `docs/roadtrip-persistence.md`.
- The Winnow client WRITES in exactly one place, the document bucket, with `If-Match`; 404 is `notfound` (never revealing a foreign row), 412 is `conflict` carrying the server's revision — `architecture.md`.
- A Studio project is kept on a Winnow by the same machinery as a trip (`shared/sources/doc-sync.ts` + `SyncPill` + `doc-remote.ts` + ONE shell hook, `use-document-sync.tsx`, since 2026-09-15; `projects/project-remote.ts` the twin of `trip-remote.ts`); its folder handle and thumbnail never travel, the media refs do, and the shell remounts the editor whenever it replaces the document under it — `studio.md`.
- A picture is reframed inside its frame — drag, zoom, rotate, flip — by ONE shared transform (`shared/media/framing.ts`) the preview, the PNG deck and the burned-in clip all draw through; under Fill it can never be zoomed out past covering or panned off an edge, and black bars appear only where the author asked for Whole (`fit: 'contain'`, v18, the maintainer reversing "never a gap" on 2026-09-13); a flip mirrors what the FRAME shows at any rotation (`flipFraming`), and a crop is never inherited by the next piece — `roadtrip.md`, `media-pipeline.md`.
- Conformed footage (slow motion, time-lapse) is corrected by ONE measured number — capture seconds per media second — applied in `attachMotion`; physics runs on capture seconds, aesthetics on timeline seconds — `studio.md`.
- The intro is a **scene** (a shared window + optional scrim + solo) over ordinary elements that gained a `window` and an `animation`, not a second class of element; it plays over the running footage and the export stays 1:1 with the source — `studio.md`.
- The DJI video `.srt` carries no battery level (Mini 4 Pro included): the gauge takes an authored value or a named telemetry key, and draws empty rather than inventing one — `studio.md`.
- A trip's dates and its route are re-edited in the CREATION sheet, which says before saving what a shorter span does to the legs (trimmed, dropped) and never touches a piece; the route writes back into the legs that hold its two ends — `roadtrip.md`.
- Road Trip tracks a journey by **calendar day, never by file name** (exports get renamed and re-graded); days are derived from the trip's two dates, and trip dates are `YYYY-MM-DD` with every subtraction in UTC — `roadtrip.md`.
- A second durable store follows the studio's hand-rolled IndexedDB pattern in its own database; Dexie and SQLite were both weighed and declined for a one-document-per-trip model — `roadtrip.md`.
- The Road Trip day badge is built from ordinary overlay `text` elements handed to `drawOverlays`, never a second renderer — it inherits the title-style presets, and preview and export are the same code at two sizes — `roadtrip.md`.
- A badge's look belongs to the TRIP, not the post (a signature that varies per post is not one), and defaults to `neutral`: flat vermilion measurably vanishes over warm footage — `roadtrip.md`.
- Badge copy is English by default and every word is an editable field on the trip; a per-piece override that is emptied returns the computed value, never a blank — `roadtrip.md`.
- A badge piece departs from the theme by writing its own value AND pinning that key in `styleOverrides`; casing is applied to the string, never to the element's flag — `roadtrip.md`.
- `StylePanel` is engine-level (`shared/overlay/`) since Road Trip became its second consumer — a tool never reaches into another tool — `architecture.md`, `roadtrip.md`.
- `LegibilityStyle` gained a corner radius and an outline, drawn by one shared helper; the radius defaults to what the four call sites used to hard-code, so no stored document changes shape — `roadtrip.md`.
- A badge can credit the CAMERA — a seventh piece under the temporal line, the lightbox's own `exposureSummary`, MEASURED from the hook picture at every render and never stored, opt-in per piece (`PostBadge.showExif`, v23) because a badge is a signature — `roadtrip.md`.
- A badge's temporal line ("515 days ago") is a MODE and a PIECE of its own, drawn under the place — it never displaces the trip's name; `anniversary` fires only on the real anniversary, and the reference day is an input, never `Date.now()` — `roadtrip.md`.
- Every mode in a Road Trip panel shows the line it would really draw for the post in hand, or the reason it cannot: a fabricated example, and a silent fallback, both read as a broken feature — `roadtrip.md`.
- The day a piece tells is measured from the picture (EXIF, else the file's date, labelled as such) and offered, never applied on its own; a picture dated outside the trip is called out — `roadtrip.md`.
- `exif-parser.ts` is engine-level (`shared/exif/`) since Road Trip became its second consumer — `architecture.md`, `roadtrip.md`.
- An overlay EXIT animation needs its window to have an END, or it never plays — the badge's hook duration is what supplies one — `roadtrip.md`.
- A glyph drawn on canvas must survive a font stack we do not control: an emoji default drew nothing at all where no colour-emoji font existed — `roadtrip.md`.
- A Road Trip post is a DECK (hook → content → call to action); a reel or a photo is the same model with one slide, and the closing card lives on the TRIP — `roadtrip.md`.
- The QR code is encoded locally (`shared/lib/qr.ts`) rather than fetched, and is verified by decoding the rendered canvas with a decoder installed in the scratchpad, never in the repo — `roadtrip.md`.
- `drawOverlays` draws one line and never wraps: a sentence is wrapped onto a character budget first (`shared/lib/wrap-text.ts`) — `roadtrip.md`.
- An animated hook burns into a clip through the Studio's own `exportVariantVideo`, trimmed to START on the chosen frame so `originSeconds` puts the entrance on frame one; the scrim reaches the frame through a new `paintUnderOverlays` hook — `roadtrip.md`, `media-pipeline.md`.
- Only a deck's content slides reorder; the hook and the call to action are structural — `roadtrip.md`.
- A hook can MOVE without an animated badge piece (Défilé): the clock, the thumbnail capture and `deckSlides`' `auto` must all read `ResolvedHook.seconds`, and a still of a hook is taken at rest past it — `roadtrip.md`.
- The **Itinerary** replaced the Route trace on 2026-09-15 (retired; a stored `route` layer is CONVERTED by the v20 migration, never dropped). It is one of the suite's two map openers, beside Virée: Virée drives a car over stops the DOCUMENT gives it (legs, or a picture's own GPS), the Itinerary draws a still map of stops the AUTHOR gives it. Against the Route it is the exact inverse: the Route reads the legs and so refuses to pin a point, while a stop here is picked, ordered and given its one picture by hand — which is what lets it mark a point and caption a photograph with a place. Picked on the export's own projection run backwards (no tiles, no basemap, no third network exception); the pen travels hop by hop with a wait at each; the drawing is CONTENT on the stage — clicked, outlined and dragged like an element, through two optional editor-only members of `HookVariant` (`frameBox`, `moveBy`) that never reach `HookRender`; the picture is pinned at the stop, on a card under the map, filling the frame or in a strip along the edge, and a stop that holds none draws NOTHING — `roadtrip.md`, `docs/hook-engine.md` §13.
- A piece's OPENER is a hook VARIANT from a registry (`shared/roadtrip/hooks/`, `PostBadge.hook` v15): `prepare()` returns a closure so the paint, the content and the sound cannot drift apart, a variant rewrites named badge PIECES instead of building elements, `owns: 'frame' | 'layer'` is declared from the start, and the field is a LIST with one entry so a stack costs no second migration — `roadtrip.md`, `docs/hook-engine.md`.
- A hook's flashed pictures are SOURCE pictures asked for by REF (`wantsPictures`), never the thumbs store — those hold finished hooks with another badge burned in, the maintainer's first report on Défilé. The shell finds each in the Library, else fetches the instance's proxy without adding it to the pool, crops to the frame at decode inside one shared pixel budget and grades it with the piece; the author picks them in a span-wide chooser where everything starts ticked — `roadtrip.md`, `docs/hook-engine.md` §9.
- The fourth opener, **Virée**, drives a cartoon Prado over a map drawn here (no tiles): stops are the legs' located places or the picked pictures' own GPS, pictures pop as prints, fill the frame or sit behind the map at each halt, and the car is a hand-rolled software 3D renderer over CONVEX parts — never a 3D library, its paint order ONE sort over every visible FACE keyed on the face's farthest vertex (ordering by part centres made whole pieces vanish at some angles), over a hull cut into convex panels at a car's real shut lines with the wheel arches left as absences — no polygon spans the model and nothing shares a volume, which is what stopped the bonnet and the flares reading as transparent; a BSP tree is the textbook cure and is ruled out because one ink stroke per face would scribble every split. The few pairs one key cannot order are then SETTLED by an exact overlap test, bounded and never splitting a polygon. Guarded by a sampled depth-buffer oracle over the angle range. The car itself is the TRIP's (`TripDoc.car`, v19, portable): the maintainer's own Prado J120 with its photographed gear, in Raptor black by default, dressed in a GARAGE — one controlled panel (a turntable drawn by the drive's own renderer, plus the car's rows) with two homes, the trip settings' Car section (live) and a modal the Virée panel opens from the piece through the host verb `configureCar` (a draft, written on Done) — `roadtrip.md`, `docs/hook-engine.md` §13.
- A clip slide stores its in point, its SCREEN time and its speed (v16); the source stretch is `[in, in + screen × speed]`, derived in `clipSlice` and never stored, so the trim bar, the band and the encoder cannot disagree. The stage plays it, the badge runs on the DELIVERED clock (`overlayClock`), and a re-timed clip ships silent, said in the plan — `roadtrip.md`, `media-pipeline.md`.
- Vignette and scrim are ONE stack of shades (direction × reach × strength × colour × invert × follow the badge's edge or anchor), the direction picked on a 3×3 glyph grid with quarter-circle corners and never a multi-select; a middle band must run edge-to-edge with the peak in the centre, or a canvas gradient blacks out the far half — `roadtrip.md`.
- In an async paint, read the canvas's size AFTER the last await: a stale render that read it before drew a miniature over a resized stage — `roadtrip.md`.
- A clip's frame picker SEEKS the open video element (`BadgeSource.seek`) and never re-decodes; the paint waits on a `frameSeq` bumped when the seek lands — `roadtrip.md`.
- A hook's frame is chosen by dragging a filmstrip; cells sample the middle of their slice, frames stream in as they decode, and the drag is throttled to one change per animation frame — `roadtrip.md`.
- Road Trip addresses everything in the hash (`#/roadtrip/<trip>/<day>/<piece>`), so Back lands on the day you were on and a day is linkable — `roadtrip.md`.
- The tool is named **Trips** on screen since 2026-09-10; `roadtrip` stays its slug everywhere it is written down — the route, the ids, the database, `.roadtrip.json`, the module paths — because a link, a backup and a briefed project all outlive a word. Memory and the design briefs keep saying Road Trip — `roadtrip.md`.
- A control about the PIECE (its name, its Studio link, the trip's defaults) must never be rendered inside the hook-only branch: on a carousel's second slide it vanishes and reads as missing — `roadtrip.md`.
- Road Trip briefs the STUDIO: a piece links a project and the badge is sent in as a `roadtrip-hook` scene, so one export carries grade + telemetry + hook; a send replaces the last, and the shades' shape does not cross over — `roadtrip.md`.
- Road Trip GRADES, through the Studio's engine and never a second one: the trip's `grade` (v10) dresses every piece, a post's `grade: null` follows it, `renderBadge` grades at source density before the crop, and the grader is caller-owned because a WebGL2 context per repaint is never reclaimed — `roadtrip.md`.
- A look is written on one of THREE rungs — the trip, a piece, or ONE PICTURE (`PostBadge.grade` / `PostSlide.grade`, v22, `shared/roadtrip/post-grade.ts`) — because a deck mixing a D-Log clip with a phone photograph cannot wear one conversion LUT. Down a rung seeds from what is shown, up drops what is below; the open picture and everything sharing its rung render through the LIVE stack (so the strength slider keeps its deferred bake) while a picture that departed renders through a second, READ-ONLY bake (`shared/lut/use-grade-cubes.ts`) over the one layer resolver (`saved-grade.ts` pure, `restore-grade.ts` fetching) — `roadtrip.md`, `media-pipeline.md`.
- The piece editor is FOUR tabs (Content · Look · Picture · Export) over one stage where a click selects a piece and a drag moves the whole block; the deck and its transport are ONE band under the picture — «Aiguille»: a fixed needle, the piece sliding under it, ▶ playing the whole piece slide after slide and LOOPING, over the piece or the open slide by a pill and `L` (`DeckStrip`, `use-deck-transport.ts`) —, everything trip-wide is behind ⚙ Trip, a mode shows the line it really draws with its list one click away, and every standing paragraph folds behind an ⓘ — `roadtrip.md`.
- `GradePanel` is engine-level (`shared/lut/`) since Road Trip became its second consumer, exactly as `StylePanel` — `roadtrip.md`.
- Choosing a look is choosing a STRENGTH: the gallery's scene carries that slider and the pick hands it over (`onPick(id, intensity)`, every `add*` taking one), the grid follows it only where it bakes live; that scene is also the one media preview that ZOOMS and pans, being a surface with no framing gesture of its own, and the modal around it is as tall as the MEASURED screen — a rem cap on a height is a rule about line length, and a grid has none — `media-pipeline.md`, `frontend.md`.
- `#/studio/open/<id>` hands a project between tools and rewrites itself on arrival; neither tool reaches into the other's state — `architecture.md`, `roadtrip.md`.
- A trip overview is a CALENDAR OF MONTHS at every width since 2026-09-22 (`MonthCalendar`, `month-grid.ts`): one block per month, seven columns Monday to Sunday, the cell a seventh of the column — ~47px on a phone, 52 at 1280 beside the library's rail — and the blocks side by side on a wide screen, as many as fit at a 34px cell. The weekday heatmap SURVIVES as the year MAP above it (read, and jumped from by month, never a target), the loupe is the SCROLL — the ruler details the month on screen and its two neighbours, ~9px a day against the dragged loupe's 6.4 —, and the ruler's drag-and-drop is kept on the wide screen. The short-trip strip and the loupe brush are gone; `visibleBlock` reads a row from its LEFT — `roadtrip.md`.
- The trip gallery has TWO views, Cards (with the cover, the default) and Bands (progress rows under a "resume" band); the choice is a `localStorage` preference, never on the document — `roadtrip.md`.
- A trip remembers the look it gives a new piece of each kind; what belongs to one day is never inherited — `roadtrip.md`.
- One trip's or one project's look can become the HOUSE STYLE every new one starts from: a committed `house-style.json` (`shared/roadtrip/`, `shared/projects/`) written by a dev-server-only endpoint (⚙ Trip → House style, Project settings → House style), never applied to an existing document, an import, a duplicate or a Trips-made project, and a picked Studio template still wins; openers declare the options that hold one piece's content (`HookVariant.contentKeys`) and what Trips sent into a project is left out, so no picture or day of one journey leaves it — `roadtrip.md`, `studio.md`, `deployment.md`.
- A Road Trip stage is a LEG carrying an ORDERED list of located places; its start and end are the first and the last, derived and never stored, and a place has no dates of its own — `roadtrip.md`.
- The legs are a horizontal RULER under the calendar grid (drag an edge or a bar, whole days only, one leg open beneath), the grid's cells wear their leg's tint, and a right-click on a day starts/ends/extends a leg by name — "start here" inside a leg is a cut. Geometry and edits are pure (`stage-ruler.ts`, `stage-edit.ts`); every drag has a keyboard twin — `roadtrip.md`.
- On that ruler a drag moves a LEG and nothing else: a day is picked with a click, never a scrub, so a finger is free to scroll a track wider than the screen — `roadtrip.md`.
- The stage ruler is ONE gesture surface: a tap opens what it landed on, a swipe travels it and glides on after the lift, and a leg is picked up by a finger only after the hold — a surface that pans TAKES the gesture (`swipeIntent` + a non-passive `touchmove`), because `touch-pan-y` alone loses it to the browser one frame in. On a phone the ruler is not drawn at all: the legs are a sheet and a leg's dates are dragged on the calendar itself, one cell one day — `roadtrip.md`, `frontend.md`.
- A scrolling tile grid pins its ROWS in pixels and never lets them be `auto`: a grid divides a height that turns definite among its auto rows, and the pixel height on the picture inside is then clipped to a sliver — `frontend.md`.
- A drag surface inside a scroll box claims only the axis it writes (`touch-pan-y`, never `touch-none`), and a surface that is merely pointed at claims nothing: `touch-none` on something that scrolls leaves touch no way in — `frontend.md`.
- The Library's instance tab can look at the day BESIDE the one a tool publishes (the stepper's arrows), as a sidebar-only override anchored to that span: it never moves the piece, and it drops by itself when another piece or day is opened. Its preview pages past a day onto the nearest day WITH media, through two edge cards in the deck — `architecture.md`.
- Looking a place up online is the suite's SECOND network exception, taken deliberately: opt-in, off by default, consent in `localStorage` and never on an exportable document, and never as-you-type — `roadtrip.md`, `local-first.md`.
- The overview's zoom pills were retired for the loupe on 2026-09-13, and the loupe brush for the scroll on 2026-09-22; `StageZoomControl` survives for the media lightbox alone — `roadtrip.md`, `frontend.md`.
- Road Trip's grid draws its own hover card (fixed-positioned, pointer-transparent) because the native `title` is far too slow to sweep a calendar with — `roadtrip.md`.
- Each post keeps a small JPEG of its HOOK in a second IndexedDB store, taken from the preview canvas, drawn at the piece's own orientation, and pruned on delete — `roadtrip.md`.
- The Studio edits **stills on the same stage as clips** — a photo is a media a project holds beside its rushes, never a second kind of project — `studio.md`.
- A compare reads BEFORE → AFTER, left to right, in every tool (2026-09-22): the picture as shot on the LEFT of the divider, the corrected one on its right — so in `useDevelopPicture` the wipe is the divider's POSITION and no split is 0, not 1. The before/after split is also a SWITCH the host owns, and it SUSPENDS itself while a mask tool holds the pointer — a picture wiped at 0.5 showed a layer's effect on one side only, and a tap that missed the frame threw the divider; the wipe is remembered, never reset. A picked subject point is DRAWN (`framePoint` + `stagePoint`, from the view's arithmetic and never a measured rect, which lags a frame): a `+` disc that becomes `−` under the pointer, over a `cursor: copy` stage, answering its own press so the stage's hit-test cannot remove it twice. A layer's visibility is a verb (an eye), never a checkbox — `develop-roll.md`.
- What TEACHES and what the picture SAYS are different texts and get different homes: the shortcuts and every gesture hint live behind `H` / `?` / a stage-bar verb (`DevelopShortcuts.tsx`), the develop's own facts are a corner stack ON the photograph toggled by `I` and off by default — a list (`developLines`) the settled row merely joins, so the two readers can never disagree; the CROP stage is the exception and keeps its line underneath, because its handles reach into every corner. Since 2026-09-22 that stack also says what the CAMERA did — `ƒ · shutter · ISO · EV` (`captureLine`, shorter than the lightbox's `exposureSummary` because the bar already names the file), read from the rendition ON SCREEN, drawn on TOP above a hairline and marked with the accent: a number the author can change must never look like one they cannot — `develop-roll.md`.
- **Develop is the THIRD editor**, and its v1 is BUILT, D1–D10 (decided 2026-09-15, finished 2026-09-16 — gallery, routes, the Home door, a stage + filmstrip + inspector that writes THROUGH with no Done, a Shift/⌘-click filmstrip selection driving apply/paste-to-selection, a Crop tab framing the DELIVERED picture over `shared/media/framing.ts` with the Develop viewport kept mounted under it, an Export tab whose long edge is a CAP and whose `Auto` fetches a decodable original only where the proxy could not fill the frame the original could — never a RAW —, originals held for the session, finals home per capture through the now-shared `SendFinalsPanel`, and a `Develop` verb under any picture looked at large that answers AFTER the render): it opens a **roll** — a document of pictures each with its develop, crop and (since roll v5, 2026-09-23, *"c'est le média qui décide"*) its OWN look, plus the roll's export — kept locally or on a Winnow like trips and projects; presets are ONE personal book shared by the tool and both modals (BUILT, D4: a live store every sheet draws, a UUID not a fixed id, a 412 merged by name and never shown); v1 has crop · straighten · flip and a JPEG export (sending a roll HOME was unplugged 2026-09-20, `develop-roll.md`), a filmstrip with batch, a histogram; no rating or flags, and "send to Trips / a project" waits for after v1. The modal is already one layout of shared workbench blocks (`shared/develop/`), and a change to developing goes into a block — `develop.md`, `develop-roll.md`, `docs/develop-tool.md`.
- The Develop tool's crop is a CLASSIC crop since 2026-09-19 (the maintainer found the D8 stage unusable): the whole picture still, a ZONE drawn over it under a veil, the picture turning under the zone from a remembered intent; the zone is converted both ways to the `aspect` + cover `Framing` the roll always stored (`crop-rect.ts`), so nothing migrated. A crop leaves at the picture's OWN density, and a picture may be delivered on a BORDER (`RollPicture.border`, roll v2: file format, colour or blur, margins on the crop's short side); crop and border have separate Apply-to verbs — `develop-roll.md`.
- A Develop roll **finds its own pictures** (2026-09-16, the maintainer's Q1–Q3): an instance's are fetched by the roll, near the open one, never into the Library; a local folder's handle is remembered per roll and re-read (one click when asked); a drop re-links; and a roll that asks keeps a 2048 px **working preview** of each LOCAL picture — the one exception to "media bytes are never persisted", opt-in, weighed, deleted when turned off — `develop-media.md`, `local-first.md`.
- A photograph is read as the **one telemetry cue it is worth** (`shared/exif/exif-cue.ts`), so the exposure, position and time elements work over it; what a still cannot answer (speed, heading) stays `—`, and a DRONE still does know its height above take-off — `studio.md`.
- A source **vouches for the EXIF its proxy dropped** (`MediaOrigin.exif`, merged UNDER the file's own by the one read path, `shared/exif/read-exif.ts`): a re-encoded rendition carries no metadata, so the values Winnow parsed at ingest are what make a drone photo's exposure, position and altitude readable at all, and a panel says which instance vouched — `studio.md`.
- A still has **no clock**: the deck is settled (`settleForStill`) before it reaches the renderer, or an entrance draws mid-slide and a later window draws nothing — `studio.md`.
- An `ImageBitmap` on the stage is released **one commit after** it is replaced, never where the new one is decoded: painting a detached bitmap throws inside rAF and kills the render loop — `studio.md`. The Develop stage (`use-develop-picture.ts`) does the same since 2026-09-21, and skips a retired source that is ALSO the one the state holds — `develop.md`.
- A RAW yields its image slot to a sidecar JPEG of the same base name — the decodable half is what every tool wants to show, and listing order must not decide it — and since 2026-09-21 the losing half is KEPT in `AssetParts.siblings` (it is one of the capture's renditions), leaving the pool with its asset through `assetFiles`; since 2026-09-23 the row SAYS it (`JPEG + DNG`, a `+DNG` chip on the cover), because a RAW folded silently into its JPEG read as a RAW the Library ignored — `architecture.md`, `studio.md`, `renditions.md`.
- A project is **intro · footage · closing card**: `ProjectDoc.outro` is a flat card of overlay elements (+ optional QR) the export keeps encoding after the footage — appended, never over it; audio ends with the footage; clean variants ship without it — `studio.md`.
- Road Trip's call to action crosses the bridge into that outro slot, same rules as the hook (prefix, replace, unlink); an outro the author composed themselves is never overwritten — `roadtrip.md`, `studio.md`.
- Anything added to a project's portable half must land in FOUR places: `ProjectPortable`, `toProjectFile`, `parseProjectFile` and `applyProjectFile` — the last was forgotten once and the gallery import silently dropped intros — `studio.md`.
- A tool that redirects from a route effect must first check the path is its own (`isWithinRoute`): a mounted tool still observes the hash after it has changed to another tool's, and redirecting then makes the switcher do nothing — `architecture.md`.
- A **source** is where projects, state and (later) scheduled work live — not a media pool. `local` is source #1 (File System Access + IndexedDB), a Winnow instance is its peer, and **a document belongs to exactly one source**: that limit is what removes sync and merge entirely. Built since phase 0.5: `shared/sources/source.ts`, `ProjectDoc.sourceId` (v14) and, since 2026-09-06, `TripDoc.sourceId` (v11) — both bound half, never in the portable file — and the studio gallery groups by source even with one group — `architecture.md`, `roadtrip.md`, `docs/winnow-bridge.md`.
- **Atelier is a client, never a host** — no backend, no database; persistent state lives in the browser or on a server the user owns. That is what makes multi-device, and later proactive features, possible without becoming a cloud — `docs/winnow-bridge.md`.
- **Atelier writes to a Winnow in three places only** (the third since 2026-09-20): finals going HOME to the instance the media came from — from the Studio alone since 2026-09-20, the Develop tool's own send being unplugged until Winnow can receive a final (`develop-roll.md`) —; a document — a trip, a Studio project, or a LUT pack's index — kept there on request (the persistence bullets below); and, new, the **file store a purchased look's lattice lives in**, keyed by SHA-256, written on "Keep on <instance>" and read one look at a time on first use (`docs/lut-packs.md` §4.4). The finals go — one button after an export, the files just rendered, `original_asset_id` alongside so the link is exact, refused up front on a viewer account / a foreign clip / a file over the upload limit. Never to a third party, never automatic — `local-first.md`, `architecture.md`.
- Connecting a source and MANAGING it are one screen (`#/sources`, answering on `#/connect` too): a row per source with its health, its capabilities, the documents this browser holds from it, and its verbs, over a connect form. Opening it re-asks each instance and stores the answer, so a stale capabilities sheet is cured by the screen that reports it — and, since 2026-09-21, by any GALLERY that would otherwise leave an instance out in silence: the probe fires only where the stored sheet is already hiding something (zero requests on a healthy gallery, measured), and an instance that still does not keep the kind is SAID rather than dropped — `architecture.md`.
- Topology is **configuration, not a decision**: `SameSite` is judged on the SITE, so `atelier.steeve.website` ↔ `winnow.steeve.website` is cross-origin but *same-site* and the cookie travels with a CORS allowlist. A Bearer credential is only needed for a genuinely foreign instance — `docs/winnow-bridge.md`.
- Media identity resolves **id → hash → name**, and the hash is Winnow's `content_hash` recomputed locally (`shared/lib/partial-hash.ts`, fixtures pinned to Winnow) — so a project survives a rename and resolves the same file from a folder or from an instance — `studio.md`, `docs/winnow-bridge.md`.
- Winnow media previews on the **proxies** (H.264/AAC/faststart, so no ffmpeg.wasm; WebP for a RAW) and the Studio export **fetches the capture before the first variant**, with a checkbox to render from the proxy instead; a variant never upscales, so the panel states what it will really deliver — `studio.md`, `docs/winnow-bridge.md`.
- On a phone a panel is a SHEET or a DRAWER, and what decides is whether its wash would lie: a sheet over the stage is right for a panel you pick FROM, a `DockedDrawer` sharing the column is right where the panel's effect is what you are watching — the Develop workbench, whose scrim tinted the very colour being set. It does not un-retire Trips' dock; it also leaves the bottom bar uncovered, so a cell can honestly be marked while its panel is up — `frontend.md`.
- A surface that answers a PINCH must refuse the browser's own: `touch-action: none` cannot reach WebKit's visual-viewport zoom, which magnifies the app and CANCELS the pointers feeding the gesture — `blockNativeZoom` (`shared/ui/native-gestures.ts`) on the element, never on the document, is the only handle. And where a pinch can still be taken away, the ± pill is drawn at every width — `frontend.md`.
- A media PREVIEW never carries a view zoom: it fits the room it is given, and the pill belongs to the day-sized zones it was asked for (Road Trip's heatmap and stage ruler), which zoom by LAYOUT size inside a scroll box, never a transform. A box that is measured must never take its size from the picture inside it, or it measures its own output — `frontend.md`, `roadtrip.md`.
- Looking at ONE picture is the ONLY other zoom: ONE lightbox for every source (an instance's rows, the Library's own files), its deck a transform and not a scroll box because it must follow a finger past its own edges — fit to 8×, drag pans when zoomed and swipes when not, a trackpad sweep pages the moment it crosses its line and swallows its momentum, and the node a wheel sweep started on is never unmounted (Chrome latches the sequence to it). Since 2026-09-21 it also SWITCHES between the capture's files (chips: `Proxy · DJI_0202.JPG · DJI_0202.DNG`, a RAW shown through the render inside it) as VIEW STATE that writes nothing — the `Develop` verb under the picture is what carries the file on screen onto the roll (`MediaAction.run(view)`) — `renditions-build.md`. The develop sheet inspects its picture the same way through its own deck-free hook (`use-picture-zoom.ts`), a drag panning once zoomed and a handle on the divider still comparing — but to **4000 %, Lightroom's ceiling**, with one preview pixel per device pixel demoted from limit to LANDMARK (2026-09-19): past it a pill draws the picture **pixels** instead of **smooth**, so a magnified preview pixel looks like a pixel rather than like detail; and since 2026-09-20 the honest cure IS there: past the stage's 1:1 a LOUPE decodes the file whole into a second grader and draws the visible window at the file's own density over the stage, released when the view comes back — `frontend.md`, `render-detail.md`.
- A toolbar never INSERTS a control: a pill drawn only past 1:1 slid every verb after it sideways, so a second press of `+` landed on it — what depends on state hangs off a label of FIXED width (`OverflowMenu`'s `bare` trigger), which is also how a file NAME carries the list of its capture's files and how the `%` carries `smooth ↔ pixels`; the Develop bar's verbs are ONE WELL of glyphs now (the clipboard's three, then the stage's own past a hairline) and its wipe says `A/B` like the Studio's — `frontend.md`, `develop-roll.md`.
- A menu is PORTALLED at fixed coordinates, never `absolute` inside its card — positioned siblings paint in tree order, so no `z-index` can lift a menu over the next card; and nothing MOVES under the pointer: a hover says shadow and border, never a translate — `frontend.md`.
- Space plays and pauses in every tool, and a control owns that key only while the KEYBOARD is on it: a button, a pill or a slider a CLICK merely left focused hands the press back to the transport (text, `contenteditable` and `<select>` keep it unconditionally). `:focus-visible` cannot answer this — Chrome grants it on the press itself — so the gesture that moved the focus is watched instead, and a window handler stands down on `defaultPrevented` — `frontend.md`.
- A status whose sentence nobody controls the length of belongs in a header row as a PILL of the same family — a dot and one word, the sentence and its verbs in a popover on click or hover — never as a row of its own; the word itself shows only where the state waits on the author, at any width, because a toolbar tight enough to wrap it is not only a phone's — `frontend.md`.
- A header bar says ONE word at most: the Trips piece bar is icons (chevron, cog, joined history, dot) plus the word Export, which folds to its glyph while a status pill speaks (`group-has-[[data-speaks]]`) and becomes its own progress fill while exporting — `frontend.md`.
- A modal is a full-screen sheet under 820px showing ONE pane at a time (drill-down, never a stack in a fixed height); `dvh` not `vh`, 16px controls so iOS does not zoom — `frontend.md`.
- Every screen with a way back opens on ONE shared band (`shared/ui/PageBar`), one pill high, so the back button's top IS the content's top on every screen and at every width; a title that needs two lines is drawn under it, never beside it — `frontend.md`.
- A tool screen locks the DOCUMENT (`data-shell="fixed"` on `<html>`: a full height, `overflow: hidden`, `overscroll-behavior: none`), because a clipping full-height frame cannot reach iOS's larger initial containing block — that gap is what slid the masthead off a phone. Reading pages keep the page scroll, and `viewport-fit=cover` is what makes the safe-area padding the shell already pays mean anything — `frontend.md`.
- That height is **measured, never `100dvh`**: the app publishes `--app-h` (the larger of `innerHeight` and the visual viewport, frozen under a pinch) and every full-screen box reads it, because a locked document is exactly where a stale `dvh` can never be corrected — on iOS it left a band of paper under the bottom bar after a reload — `frontend.md`.
- The home-screen icon is a committed PNG SET, never the SVG favicon: iOS reads `apple-touch-icon` alone (Chrome for iOS is WebKit too) and screenshots the page without it. Three SVG sources + `scripts/gen-icons.mjs` → seven PNGs, the manifest's own paths relative because Vite rewrites `index.html` and not `public/`. The portfolio-wide version of the rule is in `second-brain`'s `COMMON-PROJECT-SPEC.md` — `frontend.md`.
- From that icon Atelier launches **standalone**, so the status bar wears the paper instead of system white — a declared `theme-color` alone never bought it, only an installed launch does; the accepted cost is iOS giving a home-screen app its own storage, crossed by an export/import or by keeping the document on a Winnow — `frontend.md`.
- A tool PUBLISHES what it can start from a picture (`MediaActions`) and the shell draws those verbs in the sheet showing it large; `run` is called with the media already in the library and ACTIVE, so no verb takes a media and none writes a ref. Road Trip's three are also the day panel's own buttons: one click makes the piece and opens it — `architecture.md`, `roadtrip.md`.
- A fetched file is dated at its **capture instant**, never at the moment it was copied (`captureMtime`): `lastModified` is what every date fallback in the suite reads — `architecture.md`.
- A Winnow thumbnail is ONE component (`shared/sources/winnow/WinnowThumb.tsx`) that RETRIES: an `<img>` fires `error` once and stays black. And a derivative cached WITHOUT its CORS headers — Winnow sends no `Vary: Origin`, its own pages send no `Origin`, and Chrome's cache is shared across one SITE — fails before the request leaves the machine, per device, for a year: the cure is to REPLACE that entry (`cache: 'reload'`, `cache-heal.ts`), in the client's replay of a failed read, in the tile's first retry and in the lightbox; `?retry=N` only routes around it — `architecture.md`.
- A Winnow asset enters the library **fetched and wrapped as a `File`** (proxy by default, original on request, `.srt` alongside), vouched for with the original's hash — never streamed by URL, never hashed on its own bytes; connecting is the user's click on `#/connect`, and nothing runs at boot — `architecture.md`, `local-first.md`.
- A remote picture lost to a reload is **re-fetched from the ref's own `assetId`**, never cached: a byte cache was offered and declined, and the document already says where its bytes live. Only a connected instance, only when a piece is opened — A Develop roll does the same without the Library: it fetches the open picture and two neighbours into a pool of its own (2026-09-16, the maintainer's Q1/Q3) — `architecture.md`, `local-first.md`, `develop-media.md`.
- A Winnow **timeline chapter** and a Road Trip **stage** are the same object reached from two ends, so an import SEEDS stages and never posts, re-running it is a diff the author accepts leg by leg (never a sync), and Atelier writes back in Winnow's nouns only. The arithmetic is built and pure (`shared/roadtrip/timeline-import.ts`): a date that is not a calendar day is refused, never sliced; a title that is only the route stays an empty name; the diff matches id → span → first place so it survives re-clustering. The screens over it (`TimelineImportPanel`) tick adds and changes by default and never a drop; a timeline link names a HOST the shell resolves, never a URL it fetches — `roadtrip.md`, `docs/winnow-timeline.md`.
- **Undo is a stack of whole DOCUMENTS** (`shared/history/`), recorded by WATCHING the tool's one change funnel rather than intercepting it: nothing describes its own inverse, identity is what tells a restore from an edit (a slice assembled from several states compares `shallowSame` instead), and two edits inside 700 ms under one label are ONE step — a present is sealed when a document opens and after every undo, the buttons are always both drawn, and ⌘Z yields to a place where TEXT is typed and to nothing else — not to the `<input type="range">` a slider drag leaves focused, which is what made undo look dead in Develop. An editor with no Done (Develop's write-through) also keeps its DRAFTS level with the document both ways (`shared/develop/write-through.ts`), or an undo steps the roll back invisibly and the next nudge writes the draft back over it; and a VIEW of the document that can also WRITE it — Trips' Library tick, kept in step with the open slide both ways — follows it unless the view itself moved, or an undo is copied over before anyone sees it (`library-sync.ts`, the maintainer's "the way back does nothing") — `architecture.md`, `frontend.md`, `develop-roll.md`, `roadtrip.md`.

- The zoom gestures are ONE reading of the hand for TWO uses (2026-09-22): **Looking** (a view zoom that writes nothing — Develop's viewport and crop, the lightbox, the look scene) and **Placing** (Trips' badge stage, where the same wheel/pinch/drag move the picture's FRAMING inside its frame and write the document, and where a view zoom stays refused). `shared/ui/zoom-gestures.ts` + `use-zoom-gestures.ts` read the wheel, the trackpad pinch, two fingers and a drag once; a surface only says what the scale is under a point and what to do with a new one, and every zoom keeps the point under the hand still — `zoomAbout` for a view, `zoomFramingAbout` for a framing. Ceilings stay per surface; `Z` and the arrows belong to Looking; a value clamped only at draw time drifts, so clamp at the write; a document lags the hand, so read what the hand last wrote. Design lab: https://claude.ai/artifact/TXiBfu1pqVd5NRrs2dov9g — `frontend.md`, «Two uses, one hand», `roadtrip.md`.

- A DELIVERED picture is named EXACTLY after the picture it came from (`DJI_0101.JPG` → `DJI_0101.jpg`, no word added), never replaces a file in the folder unless asked to (`RollExport.replace`, off; a taken name is numbered `-1`, `-2` by `shared/sources/unique-name.ts`, and the probe reaches the real file system so a case-insensitive volume answers about the original), and carries the ORIGINAL's EXIF whatever its pixels came from — the block copied whole where it can be, rebuilt from its fields for a RAW, the instance's record only as a last resort (`shared/exif/stamp-exif.ts`, the writer in `exif-build.ts` + `exif-block.ts`). The maintainer pairs a source folder and a Gallery folder BY NAME, and looks a photograph up by its position and its body — `develop-roll.md`, `architecture.md`.

- A file or folder PICKER is asked for AT THE CLICK, never after a render: Chrome honours the click for about five seconds and then refuses the picker with a `SecurityError` a "dismissed" catch swallows — which is what made "Export the roll" write nothing while one picture worked (2026-09-20). `deliver-files.ts`: pick first, render, then write; a working preview is never exported; a delivered JPEG's EXIF is stamped inside the render, before any Ultra HDR container — `frontend.md`, `develop-roll.md`, `hdr.md`.

- UI foundations copy **Winnow's recipes** (one `.btn`, Tailwind's named sizes, in-repo SVG `Icons` at stroke 1.7, shared empty/loading/confirm) — the maintainer wants the two projects to look alike; the tool switcher stays a **dropdown**, never segments, because the tool list will keep growing — `frontend.md`.

- A session that changes code reports this project's state to its fiche, `projets/atelier/README.md`, in the private `second-brain` repo: the register is that folder and `PROJETS.md` there is generated from it, never Claude's memory and never `git log`. A cloud session that has no checkout of it attaches it itself (`add_repo`, 2026-09-23), because a week of sessions left the state only in chat; the fallback block goes into the PR description under `### Registre`, and no `second-brain` token is ever put in this repo. — CLAUDE.md rule 3.

## Open items (dated; remove when done)

- 2026-08-20 — `scripts/gen-luts.mjs` tells the reader to add an entry to `src/lut/builtin-luts.ts` after regenerating. That path does not exist (it is `src/shared/lut/builtin-luts.ts` since phase 0) and the manual list it describes is gone — `builtin-luts.ts` now just reads the `virtual:luts` manifest. The comment is stale in both halves.
- 2026-08-20 — `index.html` loads Space Grotesk, Instrument Serif, JetBrains Mono and VT323 (added for the Pixel CRT title style) from Google Fonts on every page load, unconditionally, while the README's "one network exception" callout names only the opt-in map tiles. Either self-host the three faces (making the offline claim literal) or widen the callout — a maintainer call, since it is a product statement.
- 2026-08-20 — `tests/` holds only `fixtures/sample.srt`; all specs live beside their source in `src/**`. Fine as is, but a future agent should not read the empty-looking `tests/` directory as "there are no tests".
- 2026-08-21 — HLG footage is silently flattened to SDR instead of being flagged. The honest, cheap version is a notice, not a feature: `telemetry-summary.colorProfile` already carries the SRT's `color_md`, so the studio could say "this clip is HLG; we work in SDR" the way the battery gauge draws "—" rather than inventing a level. The maintainer has ruled out real HDR support (see `media-pipeline.md`); only the notice is open.
- 2026-08-22 — Element timing is edited with number fields ("appears at / disappears at", plus From-playhead buttons). The natural follow-up the maintainer will want is a **lane under the TrimBar**, one draggable bar per timed element — the first thing that would make the studio feel like a timeline. Not started; the constraint to respect is that the bar splits into bands, never z-index (`studio.md`).
- 2026-08-22 — A **pre-roll / freeze-frame intro** (output longer than the source) was explicitly deferred, not rejected: the maintainer chose "over the images" for now. Half the mechanism exists since 2026-08-25 — the outro's appended tail (`export-tail.ts`) proves the encoder seam; what remains for a PRE-roll is the timestamp shift every existing frame would need.
- 2026-08-25 — The outro edits its lines as text inputs; **free element placement on a card stage of its own** (full intro parity: drag, style, animate on the card) is the agreed next step, deferred. The stage cannot scrub past the clip, so it needs a stage mode, not a longer timeline.
- 2026-09-02 — **Winnow's entry points into Atelier are undesigned.** An "Edit in Atelier" verb on an asset, a selection or a calendar day is the cheap half of the integration and the one that would make the bridge daily rather than merely functional — but the maintainer wants to think about the entry points properly rather than bolt a link on. Deferred deliberately, not forgotten. Note a local dev server cannot exercise any of it: `localhost` is cross-SITE to `winnow.steeve.website`, so the cookie cannot travel — testing needs the deployed pair, or a `winnow.localhost`-style same-site setup.
- 2026-09-03 (rev. 2026-09-06) — **Winnow's timeline has shipped; Atelier's client side of it is built and its READ path is now checked against Winnow's own code.** The brief is `docs/winnow-timeline.md`, whose status block carries the wire mapping key by key. All five phases are built (T0–T4). The first version was written against a GUESS and was wrong in every key — the route is `/api/assets/timeline`, a chapter is keyed `key`, named `name`, spans two INSTANTS with a `tz_offset_hours`, and lists bare place strings ordered by weight — so no chapter survived the boundary while every test passed against the guess, and `chapter_id` (a filter Winnow does not have, on a schema that strips unknown keys) silently listed the whole library instead of a leg. Fixed 2026-09-06: `chapterFromWire`, `localDayOf` and `chapterDays` in `client.ts`, verified in a browser against a stub. **What is still ASSUMED is only the write half**: `original_asset_id` and `chapter_id` in `client.upload`, which Winnow's upload route does not read. Chapters are **derived per request, ids not stable**, so `origin.chapterId` is the weak key and the diff's span/place fallback is the real path; `override_id` tells an authored leg from a computed one. Still Winnow's to do: those two upload fields, the "Make a Road Trip from this leg" verb pointing at `#/roadtrip/new?source=<host>&chapters=<id>`, and — noted 2026-09-09 — a **deep link that opens ONE asset in its viewer**, which no route offers today (the viewer is local state everywhere), so Atelier's "Open in Winnow" can only land on the media's session grid — `architecture.md`. `TripDoc.sourceId` — phase P0 of `docs/roadtrip-persistence.md` — landed here as **v11** (v10 went to the grade the same day); P0's gallery grouping and "Keep on" picker, P1 and P3 landed with the persistence branch (PR #71), P2 **landed on Winnow's `main`, was deployed, and the maintainer confirmed trips persisting on 2026-09-07** — the persistence is DONE end to end and `docs/roadtrip-persistence.md` is now a record, not a plan. Two localhosts are same-site, so the deployed pair was never required to test it: recipe in `testing.md`. The one gotcha that survives: a browser connected before the bucket shipped keeps a stale capabilities snapshot and hides the feature until `reconnect` — `roadtrip.md`.
- 2026-09-06 (rev. 2026-09-20) — **A piece takes the day's pictures straight from the instance**: the post's date is the query, one picture crosses per click, and it lands in the ordinary Library. That closes the two halves of the maintainer's report the re-fetch did not (picking the day by hand, and the pool keeping a day he had moved past). Since 2026-09-20 the ONE surface for it is the Library's Winnow tab — the Picture tab's own day strip (`DayFromWinnow`) was retired as the same read drawn twice, the tab being a strict superset (no double fetch, drag onto a cell, lightbox, filters, day stepper), and an "open the Library on its instance tab" seam was offered and declined — `roadtrip.md`. What is still open there: every media surface takes the FIRST connection — through one hook, `winnow/use-connection.ts`, so a second instance becomes reachable in one place, not two. The maintainer deferred multi-instance explicitly ("on verra ça après", 2026-09-07); the shape agreed for it is a tab per instance in the Library rail, with `useWinnowSource(id)` replacing the hook. **Removing a connection is no longer open**: `#/sources` lists, refreshes and forgets them since 2026-09-08 — `architecture.md`.
- 2026-09-07 — **The Library sidebar has two tabs, `Local` and the instance, and the instance's is a VIEW** — the maintainer's own design for what had become one pile: it lists what the instance holds for the span the active tool has open (`shared/sources/media-scope.tsx`: Road Trip publishes its piece's day or the overview's selected day; a `<input type="date">` when no tool says), asked live and re-asked when the span changes, so nothing accumulates and no clean button is needed. What a click fetches is an ordinary pool asset filed under that tab, never under Local (`shared/library/asset-source.ts` splits the pool by `mediaOrigin`). Day granularity for now; a stage/leg span is the next step and needs nothing remote — a stage carries its own two dates — `architecture.md`, `roadtrip.md`.
- 2026-09-09 (rev. 2026-09-13) — **Road Trip's video exports: P1–P5 of `docs/roadtrip-export.md` are built** — a slide carries its medium, its screen time and (since v16) its clip's in point and speed (v16), chosen where the piece is composed; a photo hook leaves as a painted clip, a clip slide plays on the stage and leaves cut and re-timed, and the header's one button delivers the mixed deck in swipe order. All three paths were driven end to end in headless Chromium with the encoders swapped to VP9, and **with real H.264 on 2026-09-13** in the desktop app's Browser pane (Chrome 152 encodes H.264 up to 4K there): the only defect was B-framed clips cut two frames short with their sound 67 ms off, fixed in `trimWindow` (`media-pipeline.md`). A new "the export is broken" report needs the file and the console line, not a re-audit. Still open there: the **combined reel** (one file for the whole deck, P4's last item; silent, painted timeline); the **stored `hookSeconds` is not clamped to the clip** until the bar is touched, so the plan can say "5s" over a 3s clip while the file honestly holds 3 (a write-on-open was judged dearer than the display gap); and the Studio bridge's style loss, §8, his stated second step.
- 2026-09-20 (rev. 2026-09-23: the CPU half of a RAW's cost is bounded now — `device-memory.md` — and a constrained device's export decodes to 4096 px, so its GPU targets are 9 MP at most; the GPU arithmetic below still holds for a JPEG on any device and for a RAW on a roomy one) — **A full-density export through the render core costs GPU MEMORY the cube path never did**, and it is unmeasured on a phone. One pass (the look alone) draws source → canvas with no framebuffer: 4 B/px for the source texture, as before. The moment a second pass exists — a keystone, a lens, ONE layer — the graph allocates two float16 ping-pong targets at the full size: 8 B/px each, so a 48-megapixel still costs 48 × (4 + 8 + 8) ≈ 970 MB of GPU memory for the duration of the export, and a 61-megapixel one 1.2 GB. The edge cap (`render-core.md`, «The GPU has an EDGE CAP») stops the BLACK picture but not this. The honest cure is tiled rendering (grade in bands with an overlap the warps need) or one target at a time; measure an export with a layer on the maintainer's iPhone before choosing. Related, from the audit: a LAYER's develop slider bakes its 33³ cube SYNCHRONOUSLY on every input event (`layer-render.ts` → `composeLutStack`), while the global develop has a deferred bake (`use-lut-stack.ts`); the pass cache now keeps the cube across unrelated changes, but a drag on a layer's own slider still bakes per step — a deferred bake for the layer is the next cheap win.
- 2026-09-09 (rev. 2026-09-23: the RAW side of this is addressed — `device-memory.md`; the JPEG bitmap held at full size for the Studio's photo, and the still export's full-size grader, are untouched and still unmeasured on a phone) — **A big photograph is still expensive everywhere the stage is not.** The stage now works to a pixel budget, but the still export and the frame grab build a grader the size of the whole bitmap (`photo-frame.ts`, `frame-grab.ts`), and the Studio keeps the decoded `ImageBitmap` at full size for as long as the photo is open — 194 MB for a 48-megapixel JPEG, before any export starts. Both are defensible (grading before the crop is a quality decision; the bitmap is what the export composes from) and both are untested on a phone, where the same arithmetic killed the tab. Measure an export on the maintainer's iPhone before deciding; the honest options are grading in bands, or re-decoding from the file at export time so the steady state is small.
- 2026-09-13 — **The interface audit is BUILT, all nineteen commits** (`aefe34e` → `085a5fa`): foundations (named type scale + state tokens with a lint rule, Button/IconButton/Segmented, SVG icons, shared loading/empty/confirm), shell (grouped tool menu, source pill, empty library as the rail), both galleries (whole card opens, ⋯ menu, new tile; Trips' Cards / Bands), the trip overview (heading as summary, one frame level, DateField, a strip up to 31 days, the fitted heatmap with a leg lane and the LOUPE for longer trips — L1), the piece editor (inspector on the shared controls, a deck TIMELINE under the picture), the darkroom on both editors, and the two-door home. What remains of the audit: migrating the legacy pill constants still in `SourcesScreen`, `WinnowBrowser`, `DayPanel`, `ComposerTool` and the instrument pages to `Button` (screen by screen, no design question left), and measuring all of it on the maintainer's phone — the browser pane here could not render the phone layout.
- 2026-09-13 — **The photo editor (`docs/photo-develop.md`) is under way: P1 to P4 built.** The maintainer chose `libraw-wasm` for the RAW decoder; the engine (`media-pipeline.md`), Trips' side (`roadtrip.md`, «A picture's DEVELOP is per slide»: v17, the `DevelopSheet`, the Picture tab's settled row, one cube per slide through `composeWith`) and the Studio's (`studio.md`, «A media's DEVELOP is bound-half»: `ProjectMedia.develops` v15, hash-guarded, the Grade tab's settled row, the same sheet over a photo or a clip) are in, and so are the time-savers (`roadtrip.md`, «One light on many pictures»: a session clipboard shared by both hosts, the trip's presets, apply-to verbs that write copies now while Done writes the open picture). Next: P5 (the RAW spike on his own DJI, ProRAW lossless and **ProRAW JPEG XL** files — LibRaw reads JXL tiles only when built with Adobe's DNG SDK 1.7 and the npm build likely refuses, so a refusal turns the choice into a wasm build of our own), P6–P8. Still his to decide: whether a developed picture goes home to Winnow as an edit (the Develop tool's v1 says yes, `docs/develop-tool.md`), and when the bridge carries the develop (§11). The histogram is decided and built (2026-09-15).
- 2026-09-15 (rev. 2026-09-20) — **Delivering a still from its original is DECIDED, and O1 + O2 are BUILT — in the Develop tool, in Trips' Export tab and for Studio stills** (`docs/develop-originals.md` §7, all six recommendations accepted): two axes (material = `DevelopSettings.base` chosen in Develop; pixels = `Auto · Proxies · Originals` at export — a control retired from every host on 2026-09-21, the picture's RENDITION answering instead, `renditions.md`), originals held for the session only (`shared/sources/original-cache.ts`), a never-checked RAW delivered from its render, RAW on the phone only after measuring. O2 reached Trips and the Studio on 2026-09-20 over one shared seam (`shared/develop/delivery-source.ts` + `fixedFrameDelivery` + `useDeliveryRow`), which is also what reversed the Studio's "photos never take this path" note. Still open: O3–O7, and decision 4 as written — corrected the same day, see the bullet above and §7.4. Measured argument: a landscape proxy cropped to 4:5 is already upscaled ×1.25 at a 1920 export. Never apply proxy-tuned numbers to a RAW at export without a look — the proxy of a RAW is the camera's embedded render, another base.
- 2026-09-16 — **Collage slides L1–L4 and motion M1–M4 BUILT on 2026-09-16** (engine, cell painter, v24 + renderers, the stage and the Picture tab's Layout section; one easing registry, overshoot/steps curves, `stagger.ts`, the badge's cascade at v25 — `roadtrip.md`, «A slide holds SEVERAL pictures» and its «Motion, M1–M4» paragraph); M5 (the cells arrive and leave), L5 (a collage slide delivered as a painted video) and M6 (`Scene.stagger` in the Studio — `studio.md`) built the same day, which closes the plan; deferred deliberately: clips inside cells (N decoders on a painted timeline, unmeasured on a phone) and a per-cell entrance override (not recommended). Not driven in a browser: hold-to-swap, a free print's drag, the Studio's scene cascade. The proposal as it stood: **Collage slides and staggered entrances** — three interactive artifacts carry the design, the last one the reference (the maintainer wanted the two halves in ONE workbench, since a collage is composed and timed in the same breath): *Collage in Motion* (https://claude.ai/artifact/AbjFTvgtnr9PKUvVNhT3dG — layout + picture + motion tabs over one stage, editable while it plays), over the two it merges, *Collage Frames* (https://claude.ai/artifact/K8a5Qm5e8Zs2syovTiuen8) and *Entrance Motion* (https://claude.ai/artifact/8Xzw2ncpCjFDrD7qVhR7NN). Fact they rest on: every frame in the suite holds ONE picture today (`PostSlide`, `ProjectDoc`), `compose-layout.ts` is two panes only, and the Itinerary's private `tile()` is the only per-cell painter. The recommendations: a pure `shared/media/media-layout.ts` (CSS-grid-style `areas` + tracks, insets, free prints, spacing in short-side fractions); `PostSlide.layout` (v22) where **cell 0 IS the slide** (its `media`/`framing`/`develop` unchanged, so every one-picture reader keeps working) and `layout.cells` holds the rest, extras past a template kept not drawn; stills first. Motion is additive to `animation.ts`: Cut = `none` + delay, Zoom/Pop = `scale` presets (no new ids), new easing ids (out-cubic, out-expo, back, spring, steps) in ONE registry shared with `hooks/easing.ts` (their ids kept as aliases), a pure `stagger.ts` deriving delays from boxes (ties share a rank, random seeded and stored), `inside` to move a picture within its mask. Two things the combined lab settled by building them: an EXIT cannot carry a delay (`AnimStep.delay` is IN only, by design), so a staggered exit is expressed as each item's WINDOW ending earlier and `phasesFor`'s midpoint clamp already covers the overlap; and a per-cell override is one optional `LayoutCell.enter` — offered in the lab, still not recommended for v1, since one step per slide is what keeps a deck coherent. Awaiting the maintainer: what "step" means (a cut on its beat vs a stepped curve), clips in cells, a collage under the hook's badge, whether exits ship at all.
- 2026-09-17 — **The photo editor is BRIEFED and four decisions are taken; nothing is built.** The maintainer wants to replace his Capture One 2024 licence (`docs/photo-editor.md`, written from his own list: white balance, keystone, masking / subject segmentation, lens correction, layers with opacity, HDR, auto adjust; then denoise, dust, defringe, levels, curves, stacking, vignetting, clone/heal, gradients). His four calls: **build the float16 multi-pass render core** (not "cheap wins only", not "RAW first" — both became phases of it); **the Develop tool grows** into the serious workbench while Trips and the Studio keep the global sheet (they gain rendering, not panels); **the segmentation model ships in `dist/`** from our own origin, so the README's network callout is unchanged and the accepted cost is a ~5 MB MediaPipe-class model — subject/background, never click-anywhere, and ML denoise is ruled out with it; **HDR means headroom AND gain-map output** for stills (§4.4's reversal above). Recommended and not asked: **WebGL2, not WebGPU**, because `scripts/check-shader.mjs` is the only remedy the repo has for CI's shader blindness and one shading language keeps it — WebGPU compute for denoise alone is the recorded next step if a measurement asks. Fourteen phases. **P0 (the brief) and both halves of P1 are built** — `curves.ts` + the two fields on `DevelopSettings` + the bake, then `DevelopCurve.tsx` over a pure `curve-edit.ts`, drawn by the modal and the tool alike; levels deliberately have no panel yet (`develop.md`). **P2's first commit is built too** — `auto-develop.ts` and the Auto/Levels sections: TWO verbs (tone writes levels and no colour; colour is a white balance and is wrong on a sunset), measured on the picture AS SHOT so a second press is the same answer, with a clamp said out loud; **Kelvin was CUT as a fabrication** (temperature is a gain, an 8-bit render has no as-shot white balance to offset from — it waits for RAW). P2's second commit added the EYEDROPPER, the same `whiteBalanceFor` solve on the pixel the author points at, reading the source through the viewport's own draw branch so a crop cannot misplace it. **P3 is PARTLY built**: `shared/exif/raw-probe.ts` walks a RAW's IFDs through the EXIF parser's own TIFF reader and slices out the render its camera wrote inside it, so a DNG or an ARW OPENS IN DEVELOP today with no decoder and no dependency — that spike is ANSWERED for a DJI DNG on 2026-09-20 from two of his own files — an UNCOMPRESSED 16-bit sensor plane, a 960×540 embedded render against 8064×4536, GainMap + WarpRectilinear opcodes LibRaw ignores, and the decode timings (`raw.md`, and the open item dated 2026-09-20 below); ProRAW JPEG XL and ARW still need his files. **P4's first commit is built**: the float16 multi-pass core exists (`shared/render/`, `render-core.md`) and is MEASURED pixel-identical to `lut-gl.ts` by `scripts/check-render.mjs` — the engine change was committed apart from the switch-over so it could be proved a null result first, and **`makeFrameGrader` now builds the core**, so the stage, every export and every thumbnail run on it through that one seam — sixteen call sites, none of them changed. **P5's engine is built too**: a keystone as a homography (`render/geometry.ts`, `keystone-pass.ts`) — the first pass that MOVES a pixel, which no cube could ever do — with its y convention MEASURED against the matrix on a GPU rather than reasoned about; wired in the second commit, where `makeFrameGrader` GREW an optional pass list rather than being bypassed, so a warp reaches all sixteen consumers the way a look does. **P6's engine is built**: distortion, lateral CA and vignetting as ONE radial pass (`render/lens.ts`, `lens-pass.ts`), its slider reaches bounded by the point at which the radius map would FOLD rather than by taste, and checked on a GPU against the pure module — but **no lens profiles ship, deliberately**: a profile is MEASURED calibration data and invented coefficients would be a fabricated correction that looks authoritative, so the manual sliders are the whole of it until there is real data for his gear. Wired in the second commit, where the ORDER of the warps (lens, then keystone) became one module every renderer reads — `picture-geometry.ts` — and a repaint bug already shipped with the keystone came out with it: a callback that reads fresh values through a ref STILL needs its identity to track them, or a consumer keyed on the callback never redraws. **P7 is built, both commits**: three procedural masks (linear · radial · brightness) and adjustment LAYERS whose adjustment IS a `DevelopSettings` — the very record and the very `composeLutStack`, so a local exposure, white balance and curve arrived at once and the same panel edits both (`render-layers.md`); blend modes were in the plan, are not in the ask, and were left out. They run AFTER the one cube rather than between develop and look as the brief said, so a slider does what the screen shows instead of acting in the log space a conversion LUT reads. Found on the way and fixed apart: `makeKeystonePass` mirrored y unconditionally, which is right for a canvas and WRONG for an `ImageBitmap` — every decoded photograph took its perspective correction upside down; a pass that asks WHERE a pixel is now converts with `imageUv` (`render-geometry.md`) and every geometry row of the gate runs from both source kinds. **P8 is built, three commits**: painted masks as VECTOR strokes, rasterised on the CPU into an alpha map the GPU samples — the same function the pure module defines, not an approximation, so the gate holds it to the procedural shapes' tolerance; then a grader that SWAPS its passes rather than rebuilding its WebGL2 context (without which a stroke's point-per-pointermove is no gesture at all, and which the keystone and lens drags got for nothing — it also made a pass's own textures leakable, so `RenderPass` gained `dispose`); then the gesture, over `unframePoint`, the inverse of `drawFramed` that a painted mask is the first thing to need. **P9 is built, both commits** — the maintainer pointed at his own `CostardRouge/p5-templates`, which settled three things the brief had wrong: the cost is the WASM and not the model (**16.9 MB**, not "~5 MB" — `vision_wasm_internal.wasm` alone is 9.6 MB, and the nosimd twin is deliberately not shipped); `InteractiveSegmenter` + `magic_touch`, so **click-anywhere is the feature, not the thing to avoid** (semantic segmentation answers about CATEGORIES, and a photographer pointing at the second of three people is asking about THIS ONE); and the selected object is category **0**, backwards from the obvious reading — the other way selects the BACKGROUND and looks like a working feature. Everything is served from our own origin and nothing loads at boot, so the README's network callout is unchanged; the document stores the points and the model id and NEVER the pixels — `render-layers.md`. **P10 to P13 are built (2026-09-20) — the fourteen phases are done**: the RAW develop on `libraw-wasm` behind one decoder seam with the sensor's data crossing the graph as a half-float source (`raw.md`); denoise, defringe and sharpen as four classic passes with pure twins, and the LOUPE that decodes the file whole past the stage's 1:1 (`render-detail.md`); heal, clone and dust as one patch list drawn first on the source (`render-repair.md`); the Ultra HDR JPEG whose gain map is measured from a second, darker render of the RAW and claimed only after the file is read back, with the display and the canvas detected and said rather than an HDR stage pretended (`hdr.md`). Four things stay open (§11): what "pixel piping" meant (read as pixel PEEPING, so P11's loupe), P3's JPEG XL answer, whether the develop crosses the Trips/Studio bridge, and the RAW-on-a-phone verdict.
- 2026-09-20 — **Film simulation: BOTH halves are built.** Colour (F1–F4′) and now the texture (F5, the `FilmNode`) — `docs/film-simulation.md` §7, `render-film.md`. What stays open is the maintainer's alone: a **taste pass on the six stocks' numbers** against real photographs, now with the grain and halation to judge beside the colour. Two places the node deliberately does not reach, both recorded rather than left to be found: the **Studio's stage** previews no grain (it drives `createLutRenderer` frame by frame for its split and strength, and putting the node there means the multi-pass machinery built twice — what §4.5 declined), though its exports carry it; and a **painted clip** (a hook over a photograph) has FROZEN grain, since the picture is graded once into a bitmap the painter draws every frame.
- 2026-09-19 (rev. 2026-09-21) — **Purchased LUT packs: all eight steps of `docs/lut-packs.md` §9 are BUILT, plus ★ favourites — the plan is finished.** One thing still waits on the maintainer and blocks nothing: §10's file-store per-file cap and per-user quota (proposed 16 MiB / 500 MiB), which only matters if the pack's grain overlays ever go there — it is **not** what refused the first real push, and a body over a cap never leaves the browser. **The first push against the deployed instance (2026-09-21) was refused 400 on every look — the upload was keyed on the source `.cube`'s hash while the body is the encoded lattice; corrected, and the test stub now enforces the route's rule** (`media-pipeline.md`, «A look has TWO hashes»). Everything else about the sync stays unverified against a real instance: this container cannot authenticate to one, so it was driven against a stub — which is exactly how that 400 survived to reach him. In one breath: the maintainer's pack **AUTHENTIC** by **Victor Jimenes** (25 looks, all 65³, 3.6–6.9 MB each) lives in a private VAULT — index in Winnow's document bucket (kind `lutpack`), lattices as 16-bit binary in Winnow's file store keyed by SHA-256 (its migration 0044, PR #259), cached in IndexedDB on each device; a document holds a REFERENCE to a pack look, never its lattice (his hard rule — weight as much as licence), and a purchased look is NEVER resampled; nothing of a pack in `public/`, git or a shared file; the picker is variant B (tree rail + grid, credits behind an ⓘ); thumbnails are baked once from the log + Rec.709 reference pair now in `public/reference/`. **Step 7 closed the leak that existed all along**: "Upload .cube" inlined its whole lattice as `customText` into every export and every document sync, and now writes into the vault as a look of one standing personal pack — the details, and the legacy read path that must survive, are in `media-pipeline.md`. Decisions and reasons: `media-pipeline.md`; the plan and what is left: `docs/lut-packs.md`.
- 2026-09-20 — **Deducing a trip's itinerary from the media's metadata is BUILT end to end**, decisions and reasoning in `roadtrip.md`, «The itinerary is DEDUCED from one position per day». The chain: `day-track.ts` → `segment-track.ts` → `track-chapters.ts` → the untouched `timeline-import.ts`, read from `client.geoDays()` (`GET /api/assets/geo?by=day`, **read against Winnow's own route**, not assumed) and offered by `DeduceStagesPanel` over the shared `StageDiffList`, from a `Deduce` button in the stages header. No document migration: `StageOrigin` has existed since v11. The naming is offline, from a committed GeoNames index (`public/geo/cities.json`). **What is NOT verified**: a real instance — this container cannot authenticate to one, so the whole flow was driven against a stub only (`roadtrip.md` records exactly what that proved). Run it once against the deployed pair before trusting it on the Australia trip. The second gesture, **«Situer cette photo», is BUILT too** (`locate-picture.ts` + `LocatePicturePanel` + a fourth verb on `TripOverview`'s `MediaActions`): one picture, its own EXIF, the same index and the same refusals, offering ONE edit to the leg of its own day through the stage editors that already exist — `roadtrip.md`, «Situer cette photo». Still open, and deliberately: the deduction reads only a Winnow (a local folder would need `readCapture()` over thousands of files, unmeasured, and a clip's `.srt` has no helper joining position to timestamp). The interactive lab carrying the design and its settings: https://claude.ai/artifact/MEwCk2EEhADsUu212WYcji
- 2026-09-20 — **A DJI DNG carries no usable render, and the RAW base ignores the lens calibration the file itself carries.** Measured on two of the maintainer's own `dji_fly_*.DNG` (body FC8482, detail in `raw.md`): the biggest embedded JPEG is **960×540** against an 8064×4536 uncompressed sensor — 0.52 of 36.6 megapixels, 8.4× short on the long edge. That is the whole of his "macOS shows it sharp, Atelier shows it pixelated" report: nothing in `raw-probe.ts` is wrong, the render is not in the file, and macOS is not showing a preview at all — it demosaics the sensor plane. Two calls are his. **(1) Should a picture whose embedded render is far below the stage open on `base: 'raw'` by itself?** The probe knows both sizes before any decode, and the cost is 456 ms at the stage budget once the wasm is loaded (4.4 s the first time); against it, `base` is meant to be a deliberate choice about MATERIAL and a develop with `base: 'raw'` is never a default. The cheap half either way is to SAY the size — `pictureFidelity` names the bits and never the pixels, and `DecodedPhoto.viaRawPreview` is returned by `photo-frame.ts` and read by nobody. **(2) The DNG opcodes.** `OpcodeList3` holds a 32×32×3 GainMap asking up to 5.93× (~2.5 stops) at the corners and a three-plane WarpRectilinear that is almost purely a **4.93 % magnification** (the green plane's `k1..k3` are 0 exactly; red and blue deviate by at most 1.2 px at the corner, which is lateral CA, not distortion — this lens needs no distortion correction worth a pixel); LibRaw applies neither, so `base: 'raw'` is sharper than the render and wrong — the corners are the fault, the frame is 4.93 % wider than the render's. It is also the MEASURED calibration whose absence is the stated reason P6 shipped with no lens profiles: both blobs are tiny and sit in the head `raw-probe.ts` already reads, and `render/lens.ts` already does the maths they ask for. The lab carrying the measurement and the three options, which dematrices an uncompressed DNG in plain JavaScript in the browser (no decoder, no dependency — that is what a DJI sensor plane being uncompressed buys): https://claude.ai/artifact/817bSD174Sm14QS5Mknpch
- 2026-09-21 — **`Vary: Origin` on Winnow's derivative routes is the root fix for the poisoned cache entries, and it is the maintainer's to make, in `CostardRouge/winnow`.** Atelier now survives without it (`architecture.md`, «The cache entry that fails before the request leaves the machine»), but it survives by paying one extra request per URL the first time each one fails, and only after the failure. The header costs nothing and prevents the whole thing — measured in Chromium: with it, the same sequence never fails once. The routes are the ones that answer `/api/assets/:id/thumb`, `/proxy` and `/download`. While it is missing, every entry stored by browsing Winnow itself is one Atelier will have to heal later, on every device separately.
- 2026-09-21 — **Reaching a capture's RAW when the RAW is a SEPARATE FILE is designed and not built, and four decisions are taken** (`docs/capture-renditions.md` §9, from the maintainer's ask about ARW and "tous les types d'assets"): companions yes; **the proxy is a PERFORMANCE rendition** (the thumbnail, the preview, where a picture opens — never where it is trapped); **the pill also offers the camera's own delivered file**, the JPEG or the HIF, because there are days he wants neither the proxy nor the sensor; and the engine understands the container and the codec rather than branching per camera. So it is a LIST OF FILES with the calibration rungs nested under the sensor one, not a ladder. The cheapest row is the delivered one (`fetchOriginal` moved from the export door onto the stage); the dearest is HEIF, and what decides its cost is a measurement — whether his `.HIF` embeds a full-size JPEG, which Winnow's own `extract.ts` says most camera HEIFs do. **He answered all thirty questions the same day** (§12): the choice is STORED on the document (so a phone shows the same picture without re-picking), the Export tab's value is final at export (§13.2 says what that costs and needs one more word), a local folder's siblings are scanned and shown as a tuple, and the shell's LIGHTBOX gets the switcher too — which reaches past Develop. §13 is what is still missing, five items. **MEASURED on his own files the same day** (§14, tables in `raw.md`), and it reshapes the plan: a Sony **ARW carries a FULL-SIZE embedded render** (7008 × 4672 against a 7040 × 4688 sensor) and no opcodes, so it opens at full resolution today with no decoder and offers two rungs; his **HIF is a grid of six HEVC tiles whose only JPEG is 160 × 120**, so slicing a full-size JPEG out of a HEIF does not work on that body, and Chrome 152 in his desktop app refuses HEIC/HEIF/TIFF outright — which makes the **HIF row redundant** (the ARW gives the same picture) and `libheif.wasm` a later QUALITY choice, never a prerequisite. So **a RAW yields TWO rows, its camera render and its sensor**, differing by 8.4× on the DJI and 0.5 % on the Sony. And a trap nothing had foreseen: **a derivative you wrote is not a rendition** — an export named the way this suite names one sat beside his DNG and basename grouping cannot tell it from a camera's JPEG, nor can the EXIF, since `stamp-exif.ts` copies the original's block. The measuring page is the **Rendition Inspector** (https://claude.ai/artifact/Np2XfVEHxT7n1wrXo6rkU6): a TIFF's IFDs, an ISO-BMFF's item table, every embedded JPEG by a marker walk, `OpcodeList3`, and the browser's own decoders. Three findings settle the size of it: the ladder is already format-blind (`proxy` and `gain` work on an ARW today; only `gainMap`/`gainMapWarp` need a DNG's opcodes, and `rungsFor` already offers them only where the file carries them — `raw.md`); what is hard-wired is `rawOffer`, which knows the file in hand and the proxy's own original and nothing else; and **Winnow already pairs RAW with JPEG/HEIF and already sends the companion's id, name and weight on every row we read**, so the whole of it costs no change on that side. Six phases, R1–R3 being the useful slice; **R1, R1b, R2 and R3a are BUILT** (`renditions.md`, `docs/run-sheet.md`) — the vocabulary (`shared/media/renditions.ts`, pure, tested: three roles, a separate `reach`, a RAW yielding TWO rows, an injected `canDraw`) plus the Winnow companion fields and `MediaOrigin.companion`; our exports marked `Software: Atelier` in every account and never offered as a rendition; a folder keeping every image file of a capture (`AssetParts.siblings`); and, on screen, **the fidelity chip's menu is now the LIST OF THE CAPTURE'S FILES** — proxy, what the camera delivered, the sensor with its rungs nested — the choice stored per picture (`RollPicture.rendition`), a drawable original fetched onto the stage and held, a folder's DNG beside a JPEG reachable as the sensor with no fetch, driven in headless Chromium on a JPEG + DNG + export-of-ours drop. **R3b is BUILT too**: `shared/develop/sensor-source.ts` is the ONE answer to where the sensor's data comes from (`file · sibling · original · companion`), called by the workbench and the export alike, a fetched RAW held under its own asset id — driven against a stub instance. **R4 is BUILT** (his pick on §13.2: keep the checkbox): a roll's picture leaves from its own rendition, `RollExport.originals` is off the document (v4), the panel says the run (`run-plan.ts`), one *Proxies only, for this run* switch stays and never touches the roll — driven against the stub instance, the chosen JPEG's own pixels written with no request. **R5 is BUILT too**: the three words are gone from Trips and the Studio, `Auto`'s arithmetic runs unnamed and the *Delivers* row names the original's file (`renditions-build.md`). **R6 is BUILT**: both lightboxes switch between the capture's files as view state and the `Develop` verb carries the one on screen. **R9 is BUILT**: the session cache has a device-sized byte ceiling, least recently used first (`renditions-build.md`). The plan is complete; R7 and R6′ stay unscheduled by decision. Five decisions are his (§9), the first being whether a paired capture offers its RAW at all. Two bugs found on the way are FIXED, not open: `.hif` was not a recognised image, and the image slot of a local pair went to "whichever half is not a RAW" — which on a Sony pair is the half most browsers cannot draw. What no measurement here can answer, and R6 exists for: whether LibRaw reads his A7C II's ARW, how big the render inside one is, and whether Sony's undocumented lens calibration is ever worth applying (the bar being the one the DJI GainMap cleared).
- 2026-09-21 — **Telling a person that something is taking time is undesigned, suite-wide** (`docs/progress-feedback.md`, the maintainer's ask alongside the renditions work and deliberately not about RAW): a hairline on a MEDIA's edge (a fill when the length is known, a sweep when it is not) as the passive surface, plus something naming the operation with a Cancel *"si l'utilisateur veut faire autre chose"* — which is itself the argument against a modal. The fact underneath, read the same day (`frontend.md`): a dozen modules already report progress through an `onProgress` callback and each is drawn its own way, `shared/ui/` holds no progress element at all, and — corrected the same day — the VIDEO exports already take an `AbortSignal` (the Studio draws a Cancel over it) while every fetch, the RAW decode, the roll's export loop and the deck's PNG run take none. **T1 is BUILT** (`tasks.md`): `shared/tasks/` (module state, a handle per operation, nothing drawn under 400 ms), `TaskPill` in the masthead at every width (the dot alone on a phone — question 3 answered), `TaskEdge` for a media's own tasks; questions 1, 3, 4 and 5 answered by building (pill not modal; the masthead; module state survives a route; no ceiling). **T2 is BUILT**: every fetch of a media's bytes is a task with its bytes and a Cancel (`trackedFetch`, `FetchOptions` on every thunk), the Develop stage and both lightboxes draw the edge, a modal sheet carries its own cancel because the masthead is behind it. **T3 is BUILT**: `decodeRaw` is a task whose Cancel drops the decoder's turn and never the worker, the stage goes back to its render on a cancel, the loupe's decode is a task of its own. **T4 is BUILT**: the roll's run, Trips' three exports and the Studio's are tasks; a Cancel stops between two pictures, ends the fetch or decode in flight and keeps what was rendered, said in the note. **T5 is BUILT**: the transcode and the pack import are tasks, the workbench's fetch prose and two private bars are gone, and the rule is that a surface beside the VERB stays while one that only repeats the pill goes. **The plan is complete, T1 → T5**; the five questions of §4 are answered by building (`tasks.md`).
- 2026-09-22 — **The trip overview is not usable on a phone, it is measured, and three directions are drawn — nothing is decided and nothing is built** (`docs/roadtrip-overview-mobile.md`, boards at <https://claude.ai/artifact/ESuup2fagvzyGVZBd97uP9>). At 390px a day is a **6 × 6px** cell on his 345-day trip and **7,7px** on a one-month one, against the 44px a finger needs; three surfaces scroll on three axes at two scales; and the column uses 560 of the 844px available. The two decisions all three directions share: the whole-trip heatmap becomes a **map** (read, and jumped from by month or leg — never a target, which is what keeps his 2026-09-13 refusal to drop it), and the **loupe becomes the scroll position**, retiring `LoupeBrush` and `useFlingPan` from the phone. The coupling that forces a choice: a 28px cell puts 73 days on screen, which is 4,9px a day on the ruler — so a phone gets ONE day surface (A the grid scrolled, B a month per block at 46px, C the legs as the spine), and the ruler stays on the wide screen, where nothing moves. Four questions are his, §7 — the direction, whether leg drag survives on a phone (his September ruling kept it), what the map marks, and 28px or wider — `roadtrip.md`. **He chose B the same day and B is now designed in full** (§8): the overview's real budget is **624px** (the shell's masthead and its bottom bar — which `App.tsx:175` already draws on the overview, so Trips publishes *Étapes* and *Voyage* into it — plus `--app-h` being measured, so the browser's bars are already out); the day is a 68px strip of the pieces' own hook thumbnails pulling up into a sheet; and **«Ajuster sur le calendrier» replaces the ruler on a phone** at one day = one cell = 46px, with a visible stepper as its keyboard twin. **BUILT, C1 → C7, the same day** (`roadtrip.md`, «Built so far»): the pure month grid, the calendar on the compact shell, the strip and the day sheet, the Stages and Trip cells, Adjust on the calendar, the pictures view, and the wide screen as the B6 board — blocks side by side, the day and the leg in a right column above 1180px, the ruler on the months on screen. Four of §9's questions stay his; the README's Trips section says the same as the code.
- 2026-09-22 — **The optimisation pass (PR from `claude/project-optimization-refactor-5vmpf9`): four read-only audits of Trips, Develop, the Studio and the shell, each finding verified by reading and the fixes MEASURED where a probe could** — the lazy tool chunks (`deployment.md`), the library's cover store and decode queue (`architecture.md`), the calendar's memoised blocks (`roadtrip.md`, R8), the Develop gain-map render loop (`raw.md`), the Studio autosave's three data-loss paths and the frame-gated stage paint (`studio.md`), the document-authority rules (`architecture.md`). **What the audits found and this pass deliberately left**, each verified by reading and worth doing when a measurement asks: Develop — a new WebGL2 context per global-develop slider step (`graph-grader` has no `setLut`; `lut-gl.ts:225` does), a layer's develop slider baking its 33³ cube synchronously per input event (a deferred bake like `use-lut-stack.ts`'s), the filmstrip's cells re-rendering on every tick (no `memo`, inline callbacks), a Free crop writing the whole roll per pointer move (the aspect skips the draft), the roll export holding every rendered JPEG until the end (write each as it lands), thumbnails and previews decoding whole files with two loops that restart each other (`shared/lib/decode-queue.ts` now exists for exactly this), the mask overlay swapping passes between paint and histogram. Trips — a badge drag writing the trip per pointer move and re-preparing the opener three times (`tripCoverage` ×3 per edit, `prepare()` ×3), the pictures view re-reading three months of hooks at every month boundary, the openers' per-frame allocations (`scrub-paint.ts` gradients and ticks, `drive-paint.ts` label measures, `map-paint.ts` projection), `pictureLine` in three drifting copies. Studio — each export variant re-reading and re-parsing the whole file, the selection outline laying every element out twice per frame (`measureOverlays` after `drawOverlays`), a big photo downscaled from full size on every redraw, the export's 5 ms polling timers stalling in a hidden tab, the glow scratch canvases that only grow, the clip header overflowing 390px, the legacy overlay page copying the Studio's element helpers. Shell — no list virtualisation (one `IntersectionObserver` per row, 2 000 rows in `WinnowBrowser`), the browser's Cancel not aborting an add in progress and list requests never aborted, publishers that also subscribe (`section-rail`, `media-scope`), the sheet drag animating `height` per pointer move, sidebar add errors swallowed and a `busy` that can stick. The PostEditor's per-frame render was profiled and left (`roadtrip.md`).
- 2026-09-23 — **What stands between Develop and leaving Lightroom / Capture One is AUDITED, not decided** (`docs/lightroom-gaps.md`, the maintainer's ask after the v5 look fix): two real bugs (a subject mask is missing from the exported file; importing one `.roll.json` twice makes two rolls share picture ids), nine consistency gaps of the look's kind (three different "edited" tests, a clipboard and presets that carry the develop only, Apply-to for four of nine groups, no picture reset, roll-wide undo, tool sizes reset per step…), and the missing tools by daily impact (HSL, texture/clarity/dehaze, colour grading, clipping overlay, mask combining, Kelvin, ICC/P3, TIFF, GPS removal, export presets, grid, virtual copies, snapshots, IPTC). §6 proposes five passes in commits. **Pass 1 is BUILT** (subject masks in the export, fresh picture ids on import, `pictureEdits`, tool sizes kept across pictures); the interpolation item was withdrawn as a recorded decision. **His answers (§8)**: Winnow's picks/stars read-only in Develop YES; TIFF NO for now; **Lensfun YES, profiles fetched on demand per lens and kept locally, never the whole database shipped**; a roll default look NO; virtual copies (C1's variants) YES. Two asks are PROPOSED, not decided (§9, lab https://claude.ai/artifact/SgJpjQtNXF62Quott5ahkk): a per-picture delivery state `auto · yes · no` set from a table in the Export tab, and the metadata a file carries — signature always on (`software-mark.ts` reads it), identity-level author/copyright, per-picture title/caption, a grouped checklist with three presets, and an offline place name.
- No secret has ever been tracked in this repository (checked 2026-08-20 across the working tree), so there is nothing to rotate.

## Topic files — read before touching the area

| File | Read when you touch… |
| --- | --- |
| `docs/memory/architecture.md` | the shell, the tool registry, the asset library, adding or removing a tool |
| `docs/memory/media-pipeline.md` | video decode/encode, exports, transcoding, LUT rendering, canvas compositing |
| `docs/memory/frontend.md` | UI, layout, styling, MapLibre panes, the design tokens |
| `docs/memory/deployment.md` | `vite.config.ts`, CI, GitHub Pages, `public/luts/`, anything about how the site is built |
| `docs/memory/testing.md` | tests, what is testable, how to keep new logic testable |
| `docs/memory/local-first.md` | anything that could touch the network, read files, or persist data |
| `docs/memory/studio.md` | the Studio tool, the tool-merge plan, project persistence, title styles, retiring a legacy tool |
| `docs/memory/roadtrip.md` | the Road Trip tool, trip/day/post model, day badges, publishing cadence and strategy |
| `docs/memory/develop.md` | `src/shared/develop/` (the workbench blocks), the Develop modal in Trips or the Studio, the decision to make the Develop tool |
| `docs/memory/develop-media.md` | where a Develop roll's picture BYTES come from — the roll's own fetch from Winnow, local files, anything that would make a roll fetch or keep media |
| `docs/memory/develop-roll.md` | the Develop tool (`src/tools/develop/`), the roll document, the personal preset book, the editor's rules |
| `docs/memory/render-core.md` | `src/shared/render/` (the float16 multi-pass graph), `lut-gl.ts`, the graders |
| `docs/memory/render-geometry.md` | the lens, the keystone, `picture-geometry.ts` — anything that MOVES a pixel, and the `imageUv` rule |
| `docs/memory/render-layers.md` | masks and adjustment layers (`render/mask.ts`, `layer-pass.ts`, `develop/layer.ts`) |
| `docs/memory/raw.md` | `src/shared/raw/` (libraw-wasm), `DevelopSettings.base` / `rawGain`, the graph's half-float source — which BYTES a picture is developed from |
| `docs/memory/device-memory.md` | `lib/device-class.ts`, `raw/raw-budget.ts`, `raw/decoded-cache.ts`, `sources/held-budget.ts`, the decoder's lifecycle — anything that allocates by the megabyte on a phone, and what a RAW costs there |
| `docs/memory/renditions.md` | `shared/media/renditions.ts`, `MediaOrigin.companion`, `materialize.ts`, `AssetParts` — which FILE of a capture a tool works from, and what each one can give |
| `docs/memory/renditions-build.md` | the rules each renditions phase fixed (R1b → R5): `sensor-source.ts`, `run-plan.ts`, `DevelopBase.tsx`, `deliveryFor` / `useDeliveryRow`, `RollPicture.rendition`, the *Delivers* row's words |
| `docs/memory/render-gain-map.md` | `render/gain-map.ts`, `gain-map-pass.ts` — a DNG's own shading correction, and any gain that is a GRID rather than a radius |
| `docs/memory/render-detail.md` | `render/detail.ts`, `detail-pass.ts`, the Detail tab — denoise, defringe, sharpen, any pass reading a NEIGHBOURHOOD |
| `docs/memory/render-repair.md` | `render/repair.ts`, `repair-pass.ts`, `RollPicture.repair`, the Repair section — heal, clone, dust, anything that COPIES pixels from one place to another |
| `docs/memory/render-film.md` | `render/film-pass.ts`, `shared/film/film-texture.ts` / `film-noise.ts` / `film-grain.ts` — grain, halation, `RenderPass.prepare`, any spatial effect belonging to a LOOK |
| `docs/memory/hdr.md` | `src/shared/hdr/` (the gain map, the Ultra HDR container, the display probe), `RollExport.hdr`, any claim about HDR on a still |
| `docs/memory/tasks.md` | `src/shared/tasks/`, `TaskEdge`, `TaskPill` — progress, a cancel, a spinner, a "please wait" anywhere |

Not memory files, but read them before starting new work, or before touching
anything about media sources or document storage:

- **`docs/roadmap.md`** — twelve proposed features (2026-09-07), each traced to
  the open item, deferred decision or stated direction it comes from, with its
  constraints, deliverables and size. **A proposal, not a set of decisions**:
  nothing in it has been agreed with the maintainer, so read it to find the next
  piece of work and its rules, never as an authority on what was chosen. It also
  carries a table of ideas deliberately left off, which duplicates no memory
  entry but points at them.
- **`docs/winnow-bridge.md`** — the agreed design for connecting Atelier to
  Winnow (the maintainer's media-triage project), the two adapter seams it rests
  on, and the phases. Verified 2026-08-29 against the Winnow repository itself
  (`~/Documents/GitHub/winnow`, `CostardRouge/winnow`), so its claims about both
  sides carry paths and can be re-checked; rewritten 2026-08-31 around the source
  model, with several instances and scheduling marked *later* but designed for.
  **Phases 0, 0.5 and 1 are built**; 2 (write-back) and 3 (remote documents) are
  not.
- **`docs/roadtrip-editor.md`** — the agreed plan (2026-09-06) to give the Road
  Trip piece editor the Studio's shape: six tabs instead of the 16-section
  accordion, click-to-select and block-drag on the badge stage, and **grading
  from Road Trip through the Studio's own engine** (`useLutStack` +
  `makeFrameGrader` + `exportVariantVideo` — no second exporter, which is what
  the earlier rejection was actually about). Four phases, one commit each.
  **All four phases are built (2026-09-06)**; the decisions they fixed are in
  `roadtrip.md`. Unverified in the container: the hook clip's graded encode
  (no H.264 encoder here) — the PNG deck and the preview were checked.
- **`docs/roadtrip-export.md`** — what a Road Trip piece can and cannot deliver
  (2026-09-09), and the plan to make an animated piece leave as a video from
  Road Trip itself. **A proposal, not a set of decisions**: §2–§3 are traced to
  files and are fact, §4 onwards awaits the maintainer, and §9 lists the four
  choices to make first. Read it before touching `use-post-exports.ts`,
  `deck-export.ts`, `hook-video*.ts` or anything that encodes frames without a
  source clip.
- **`docs/roadtrip-overview-mobile.md`** — what the trip overview measures on a
  phone (2026-09-22) and three directions out of it. §1–§2 are fact, traced to
  `day-grid.ts`, `ShortDayStrip.tsx` and `stage-ruler.ts` and measured at
  390px; §3 onwards is the design he chose (B) and §8 its full shape — **all
  of it built on 2026-09-22**, §9 being what is still his. Read it before
  touching `MonthCalendar.tsx`, `YearMap.tsx`, `DayStrip.tsx`,
  `LegsSheet.tsx`, `StageRuler.tsx` or `month-grid.ts`.
- **`docs/hook-engine.md`** — the agreed design (2026-09-13) for many hook
  variants over one badge: the contract, the three decisions that shaped it,
  the picker, how a synthesised sound bed reaches the MP4 through the export
  that already exists (the `p5-templates` pattern minus the server), the
  missing `exportGeneratedClip` seam, and seven phases of one commit each.
  **Every phase is built** (engine, picker, Défilé and its ticks — mixed into a clip's own sound on request — and the route trace, which changed nothing in the contract but one optional context field and was then retired by the Itinerary); phase 4 turned out to exist already as `encodeFrames`. Read it before touching
  `shared/roadtrip/hooks/`, `badge-layout.ts` or anything that would add audio
  to an export.
- **`docs/winnow-timeline.md`** — what Winnow's forthcoming timeline (media
  grouped into chapters by place and date) means for Atelier: the chapter ↔ stage
  mapping, ingesting by chapter, seeding and completing a Road Trip, the finals
  going home, and the list of questions to check the spec against. Written
  2026-09-03 from Atelier's side alone, so its Winnow claims are assumptions and
  are marked as such. **All five phases are built on Atelier's side (2026-09-06)**,
  verified against a stubbed instance only. **Winnow's timeline shipped the same
  day** and its chapters turn out to be derived per request (ids are not stable)
  — the brief's status block and §7.1–7.2 record what that changes.
- **`docs/roadtrip-persistence.md`** — the agreed design (2026-09-06) for
  keeping a Road Trip on a connected Winnow so it resumes from another device:
  bridge phase 3 applied to `TripDoc`. Four decisions are fixed there (local
  write unchanged + remote flush on idle with a status pill; a generic
  `kind: trip | project` bucket with trips first; own docs for any signed-in
  role, scoped by `user_id`, foreign rows 404; thumbnails re-baked, never
  uploaded), plus the security answer, the Winnow migration `0041` + route
  pair, and five phases. **All of them shipped, and the maintainer confirmed
  trips persisting on the deployed pair on 2026-09-07** — read it now for the
  four decisions and the security answer, not for a plan. Projects (P4) ride
  the same bucket. Never exercised: a network drop and a 401 mid-edit.
- **`docs/photo-develop.md`** — the design (2026-09-13) of the photo editor
  the maintainer asked for: per-picture corrections (exposure, brightness,
  contrast, highlights/shadows/whites/blacks, temperature/tint, saturation,
  vibrance) as a **develop** distinct from the grade, applied as the FIRST
  stage of the one baked cube so all six renderers get it with no shader
  change; a RAW (DNG) developed at decode into the picture the cube then
  takes, behind one decoder seam; ONE sheet opened from the Trips Picture tab
  and the Studio Grade tab, no ninth tool; presets and apply-to verbs as the
  time-saver. **A proposal, not a set of decisions**: §2–§3 are traced to
  files and are fact, §4 onwards awaits the maintainer, §11 lists the seven
  choices (the decoder is the one real dependency decision). Read it before
  touching `lut-stack.ts`, `use-lut-stack.ts`, anything about RAW files, or
  before adding a tone/colour control anywhere.
- **`docs/develop-tool.md`** — the plan (2026-09-15) for the Develop tool, the
  third editor: what the modal already shares (§1), the maintainer's four
  decisions (§2), the roll document and the personal preset book (§3–§4), the
  screens, the seams still missing (a framing-aware still export, the finals
  panel in `shared/`, and extracting the document plumbing at this third
  consumer), phases D1–D10 and what is still open (§8). Read it before
  building anything in `shared/develop/` or a new document kind.
- **`docs/lut-packs.md`** — the agreed plan (2026-09-19) for purchased LUT
  packs: a private vault on Winnow (document index + a new file store +
  a local cache), the author-grouped tree picker, pre-baked thumbnails, and
  the fix for "Upload .cube" inlining lattices. **Decided, not built.** Read
  it before touching `GradePanel`, `LutGalleryModal`, `saved-grade.ts`,
  `restore-grade.ts`, "Upload .cube", or anything that stores a LUT.
- **`docs/develop-originals.md`** — the proposal (2026-09-15) for developing
  and delivering from the ORIGINAL rather than Winnow's proxy: two axes kept
  apart — *pixels* (proxy 2048 ↔ full size, exact, chosen at export per piece,
  `Auto` fetching an original only where the proxy crop would upscale) and
  *material* (8-bit render ↔ 16-bit RAW, NOT exact, a `base` on the develop
  chosen in the sheet and never switched by the export). **Decided
  2026-09-15** (§7), not built. Read it before touching `MediaOrigin`, the Develop
  sheet's fidelity chip, or any export that could fetch an original for a
  still. It refines P5–P7 of `photo-develop.md`.
- **`docs/capture-renditions.md`** — the proposal (2026-09-21) for reaching
  the RAW of a capture whose RAW is a SEPARATE FILE (Sony `.ARW` + `.HIF`,
  DJI `.DNG` + `.JPG`), and for saying which bytes a tool works from in ONE
  vocabulary — a capture's *renditions*, three roles (`proxy`, `delivered`,
  `sensor`), the four-rung ladder derived from them rather than parallel to
  them. §2 and §10 are fact, traced to files in this repo AND in Winnow's;
  §9 is what he decided, §11 the thirty questions still open. **Read it before touching
  `MediaOrigin`, `materialize.ts`, `AssetParts`, `PictureWorkbench`'s
  `rawOffer`, or anything that decides which file of a capture is opened.**
  It refines `develop-originals.md` §7 and P10 of `photo-editor.md`; it
  revises neither's decisions.
- **`docs/run-sheet.md`** — one line per commit across the renditions and
  progress plans (2026-09-21): what is built, what is next, what is blocked and
  which four questions are the maintainer's. An INDEX, not a plan — the
  reasoning stays in the two briefs. Read it to find the next piece of work;
  keep it current or delete it.
- **`docs/progress-feedback.md`** — the proposal (2026-09-21) for saying that
  something is taking time, across the whole suite: one task registry
  (`shared/tasks/`), two surfaces (a hairline on a media's edge, a pill with a
  popover and a Cancel), five phases and five open questions. §2 is fact — the
  dozen `onProgress` callbacks already in place, the absence of any progress
  element in `shared/ui/`, and the absence of any `AbortSignal` anywhere. Read
  it before adding a spinner, a bar or a "please wait" anywhere.
- **`docs/photo-editor.md`** — the brief (2026-09-17) for what comes after the
  Develop tool's v1: the maintainer's ambition to replace Capture One, the
  **render core** that his list requires (a float16 multi-pass GPU graph in
  which today's cube survives as two nodes), the edit-stack model (layers whose
  adjustment IS a `DevelopSettings`, vector brush strokes, a segmentation raster
  cached and never stored) and fourteen phases. §1–§3 are traced to files and
  are fact; §4 records the four decisions AND the HDR reversal for stills; §10
  is what was weighed and declined, including four things a browser will not do.
  **Read it before touching `lut-gl.ts`, `frame-grader.ts`, `held-grader.ts`,
  `roll-types.ts`, or before adding any adjustment that needs to know WHERE a
  pixel is.** It supersedes neither `photo-develop.md` (the develop record) nor
  `develop-tool.md` (the roll), and it is a brief, not a built thing.
- **`docs/lightroom-gaps.md`** — the audit (2026-09-23) of what Develop
  still lacks against Lightroom Classic and Capture One: bugs, consistency
  gaps, tools, output, workflow, a proposed order and his five questions. Read
  it before picking the next piece of Develop work.
- **`docs/film-simulation.md`** — the brief (2026-09-16, adjusted 2026-09-17
  against the photo editor) for a Fujifilm-style film simulation: why its
  colour is a generated LOOK LAYER and not a stage (F2 — ordering against a
  conversion LUT), the emulsion model in stops (§5: per-channel softplus
  curves re-speeded to an exact mid grey, coupling, DIR inhibition, a paper
  stage), the texture design the future `FilmNode` builds to (§6), the lattice
  measurement that fixed 33³ (§8), and the three requirements it hands to
  `photo-editor.md` P4 (§10). Read it before touching `shared/film/`, before
  adding a look that is not a `.cube`, or before building grain, halation or
  any spatial effect anywhere.
